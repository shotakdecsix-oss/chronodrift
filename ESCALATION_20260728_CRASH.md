# 相談: 3Dブラウザアプリが数分でタブごと落ちる（原因未特定）

## お願いしたいこと

`C:\Users\Shoichi\Desktop\isehara-game`（three.js製の3D地図探索アプリ、Renderにデプロイ）が、**2〜4分でタブごと落ちます**。原因を絞り込むために大量の計測を行い、主要な仮説を10個ほど実測で潰しましたが、**まだ止まっていません**。

行き詰まっているので、**(A) 見落としている仮説**、**(B) 次に取るべき測定**、**(C) 8GB/内蔵GPU級のマシンでこの規模を成立させる設計**について意見をください。

**重要な前提**: 「増えている数字を探して上限をつける」というアプローチを6巡繰り返しましたが、毎回別の犯人が見つかるのにクラッシュは一度も止まっていません。同じことを繰り返す提案は避けてください。

---

## 1. アプリの概要

- 単一ページの three.js アプリ。`index.html` + `js/legacy/part1〜10.js`（classic script、合計約11,000行）
- OpenStreetMap（Overpass API）から道路・建物・土地利用をタイル単位で取得し、国土地理院の標高タイルで地形を作り、リアルタイムに3D世界を生成しながら探索する
- サーバーは Node の薄いプロキシ + ディスクキャッシュ（Render無料プラン、512MB）
- 主な再現地点は東京（超高密度）

## 2. 症状

- **移動していなくても2〜4分で落ちる**（当初は「移動し続けると落ちる」と思われていた）
- **落ちる時と落ちない時がある**（同じ操作・同じ場所でも）
- **クラッシュ画面にメッセージが一切出ない**
- **`chrome://crashes` に記録が残らない**（当日何度も落ちているのに、最新の記録は3日前）
- PC Chrome / スマホChromeウェブアプリで発生。PC Edge / スマホChromeタブでは発生しないという観測があるが、**後述の理由でこの差は信用していない**

## 3. 環境

**PC（主な再現環境）**
- Windows 10 / メモリ **8.0GB** / SSD
- GPU0: Intel Iris Plus（内蔵） / GPU1: **NVIDIA GeForce MX**（VRAM 2GB級と思われる）
- Chrome（バージョン未確認）

**クラッシュ時のタスクマネージャ（PC再起動後の実測）**

| 項目 | 再起動前 | 再起動後 |
|---|---|---|
| メモリ使用 | 7.5/7.8 GB (96%) | **7.4/7.8 GB (95%)** |
| 利用可能 | 316 MB | **362 MB** |
| コミット済み | 24.8/31.8 GB | 12.0/31.8 GB |
| ページプール | 928 MB | 329 MB |
| 非ページプール | 1.6 GB | 767 MB |
| CPU | 82% | 74% |
| GPU1 (NVIDIA) | 71% / 77℃ | **98% / 78℃** |

**Chromeのプロセス内訳（クラッシュ時）**
- GPUプロセス: **約1.5GB**（毎回ほぼ一定）
- レンダラ: **0.6GB〜1.6GBと不定。1GB以下でも落ちるし、1.6GBでも落ちないことがある**

## 4. 入れた計測器（すべてデプロイ済み・実測値つき）

### `[mem]`（2秒ごと、前回差分つき）
`renderer.info` の geo/tex/prog/calls/tri、`scene.children` 数、各種レコード数、`performance.memory` の used/total/limit を1行で出力。🩺ボタンONで画面左上にも表示（スマホ用）。

静止中の実測（東京・PERFプリセット high、bMax=25,000）:
```
geo 4043  tex 668  prog 57  calls 15,393  tri 2,804,333  sceneCh 64,431
bRec 23,757  dormant 60,000  roadRec 162,496  roadMesh 15,627
ways 197,828  roadG 60,624  facade 411
heapMB 279〜763  heapTotMB 465〜819  heapLimMB 4076
```

### `[gpuBytes]`（10秒ごと）
シーンを走査したジオメトリ／テクスチャの**実バイト数**、ドローイングバッファのサイズ、および **WebGLの `bufferData`/`deleteBuffer` をラップして測った「GPUに確保済みバッファの実バイト数と本数」**。

```
geoMB 42.4  texMB 54.5
glBuf 12,406本 / 17.8MB / 平均1,501B
drawBuf 1119x915  dpr1.00  aa:off  fbMB 11.7
meshes 64,432  uniqGeo 16,211  uniqTex 866
```

### その他
- `webglcontextlost` / `webglcontextrestored` の検知（発火したらタブのタイトルを書き換える）
- `killGL()` — `renderer.forceContextLoss()` を呼んでWebGL資源を全破棄する切り分け用
- `freezeWorld()` / `freezeRender()` — 生成・破棄側と描画側を切り離す二分探索用
- `setGfx(aa, maxPx)` — アンチエイリアスとドローイングバッファ上限のA/B

---

## 5. 実測で潰した仮説（これが本文書の主眼）

| # | 仮説 | 潰した根拠 |
|---|---|---|
| 1 | three.js のジオメトリ／テクスチャ量が多すぎる | 実バイト計測で geoMB 42 + texMB 55 = **約100MB**。PERFプリセットを high→std→lite と下げても落ちる |
| 2 | 既定ドローイングバッファ（MSAA × devicePixelRatio） | 実測 **1119x915 / dpr 1.00 / fbMB 11.7**。MSAAを切っても変化なし |
| 3 | `dispose()` 漏れでGPUバッファが残っている | GLの `bufferData`/`deleteBuffer` を直接ラップして計測。**glBuf 17.8MB** に対しシーン走査の geoMB 42.4。むしろGL側の方が小さく、未解放の蓄積は無い |
| 4 | V8ヒープのOOM | `heapTotMB` 465〜819 に対し `heapLimMB` **4076**。まるで届いていない |
| 5 | DevToolsのコンソールログ保持 | **DevToolsを一度も開かずに再現** |
| 6 | GPUプロセスの1.5GBはこのページのWebGL資源 | **`killGL()` でコンテキストを全破棄しても1.4〜1.5GBのまま** |
| 7 | Windowsカーネルのプールリーク | 再起動で非ページプール 1.6GB→767MB に正常化。**それでもメモリ95%・落ちる** |
| 8 | 直前に追加した新機能（BIRDモード・GPS追従・経路シミュレーション） | 機能追加前のコミット（7/26 `26d9b18`）を一時デプロイして**二分探索で再現**。旧版でも落ちた |
| 9 | 「移動し続けること」が発火条件 | **静止したまま2〜4分で落ちる**ことを確認 |
| 10 | GPUドライバのTDR（タイムアウトリセット） | `chrome://crashes` に記録が残らない＝異常終了ではない。`webglcontextlost` も未検知 |

### 個別に見つけて修正したリーク（いずれも実在したが、クラッシュは止まらなかった）
- `dormantGrid` の無制限増加（0→22,000件で単調増加）
- `roadRecords` / `roadGrid` の無制限増加（86,000→193,000件、2秒で+13,000）
- デバッグ用平面の破棄漏れ
- 国土地理院タイルの `ImageBitmap` 未 `close()`
- 先読みのバックプレッシャー欠如（`pendingBuildings` が168,000件に到達）
- 自販機・ガードレール等の小物撤去、高架の距離アンロード追加

---

## 6. 今回のセッションで見つけて直したもの（効果は実測済み・でもクラッシュは継続）

### (a) サーバーの512MB OOM
`server.js` が**キャッシュ可否を判定するためだけに**上流のOverpass応答を丸ごと `JSON.parse` し、さらにキャッシュ書き込みで `JSON.stringify({...body})` と本文をもう1本複製していた。

- 実測: 9MBの応答1件あたり RSS **+71MB**（parse 44MB / stringify 21MB）
- ミラーのワーカーを2→8に増やしていたため 71MB × 8 ≈ 570MB で512MB上限を突破
- 修正: キャッシュを `<hash>.meta`（極小JSON）+ `<hash>.body`（生バイト）に分離し、完全性検証を `JSON.parse` 無しのバイト走査に置換 → **+71MB → +6MB**
- **副次的に重要**: クライアント側の `/api/overpass 502/503` 連発と「生成が完全停止する」症状は、サーバーがOOMで死んで再起動していた二次症状だった。**「PC EdgeとPC Chromeで挙動が違う」という観測は、この交絡だった可能性が高い**（Edgeで試した回はサーバーが死んでいて生成が止まっていた）。同様に「スマホのタブとPWAで違う」という観測も、まだ検証されたものとして扱っていない

### (b) 建物ジオメトリの共有化
建物1棟ごとに `new THREE.BoxGeometry(w,h,d)` していたのを、屋根と同じ「単位サイズの共有ジオメトリ + `mesh.scale`」に変更。棟ごとに違うのは窓タイルのUV繰り返し数だけなので、量子化してキャッシュ。

| | 前 | 後 |
|---|---|---|
| glBuf 本数 | 33,132 | **11,658** |
| 1本あたり平均 | 667 B | 1,658 B |
| uniqGeo | 37,572 | **13,288** |
| 共有ジオメトリ種類数 | — | 279 |

（この変更は「D3D11/ANGLEはWebGLバッファ1個ごとに最小64KBを確保するので、コストはバイト数でなく本数で決まる」という仮説から入れたもの。33,132×64KB=2.07GB が実測GPU約2GBと一致したため。ただし後に `killGL()` でGPUメモリが下がらないことが判明し、**この仮説は根拠を失っている**）

### (c) `pendingBuildings` の素通り（静止中クラッシュの主因の一つ）
タイル取り込み時に、**実建物を距離に関係なく全部 `pendingBuildings` に積んでいた**。東京では取得半径内に15万件あるが、生成距離に入るのは2万件程度。残り13万件は生成ループが1件ずつ pop して距離で弾いて dormant へ移すだけの素通りだった。

静止中の実測ログ（プレイヤーは完全に停止）:
```
pendB  153,923 → 134,739 → 116,783 → 98,205 → 79,605 → 61,605 → 43,005 → 24,405 → 6,405
dormant  1,441 →  21,651 →  40,043 →  58,203 →  59,988 →  64,640 →  69,000 → 60,000
bRec    25,022 →  22,959 →  23,278 →  23,757 →  23,157 →  23,317 →  23,757 → 23,157
evicted/win dorm  0 / 0 / 0 / 16,815 / 13,788 / 13,800 / 27,600
```
毎2秒ぴったり18,600件が流れ、ほぼ全量が dormant へ移り、dormant は上限60,000に張り付いて2秒あたり1.4〜2.8万件が捨てられていた。`bRec`（実際に存在する建物）は23,000台のまま不変。

- 修正: 投入時点で距離判定し、範囲外は最初から `dormantAdd` へ
- 効果: **レンダラプロセスが1.5GB → 0.6GB に低下**。ただしクラッシュは継続

---

## 7. 現在の状態（未解決）

- PC再起動でカーネルプールは正常化したが、**物理メモリは依然95%（利用可能362MB）**で落ちる
- **GPUプロセス1.5GB の正体が不明**。`killGL()` で下がらないので「このページのWebGL資源ではない」と読んだが、Chromeのアロケータがプールとして抱えたままOSに返していないだけの可能性も否定できていない
- **NVIDIA GPUが98%使用率・78℃**。`calls 15,393/フレーム` は明らかに過大
- レンダラのメモリはクラッシュと相関しない（1GB以下でも落ち、1.6GBでも落ちないことがある）
- **`freezeWorld()` / `freezeRender()` の二分探索は未実施**

### 手を付けていない最大の塊
```
calls      15,393 / フレーム
roadMesh   15,627  （道路1本ごとに専用 BufferGeometry: position / uv / index の3バッファ）
sceneCh    64,432  （シーン内オブジェクト数）
```
道路をタイル単位でマージすればバッファ本数もドローコールも桁で落とせるが、道路は「地形更新で個別に `_dirty` 再構築」「距離で個別にアンロード」という設計になっており、そこの作り替えが必要。

---

## 8. 相談したい具体的な論点

1. **メッセージもクラッシュ記録も残らないタブ終了の正体を、どう確定させるか。** Chromeのメモリ逼迫による意図的なterminateだと読んでいるが、それを裏付ける／否定する方法は？（`chrome://discards`、`--enable-logging`、memory-infra トレース等、実務的に効く手順を知りたい）

2. **`killGL()` でGPUプロセスが下がらないことを、どう解釈すべきか。** 「本当に他所のメモリ」なのか「解放済みだがOSに返していない」のかを区別する方法は？

3. **物理8GB・VRAM 2GB級のマシンで、64,000オブジェクト・15,000ドローコール・280万三角形をリアルタイム生成しながら成立させるには、どこから手を付けるべきか。** 道路マージ（ドローコール削減）が最優先という読みで合っているか。

4. **道路のマージ設計。** 「地形更新で個別に再構築」「距離で個別にアンロード」という既存の要件と、タイル単位のマージをどう両立させるか。マージ済みバッチの部分更新をどう扱うのが定石か。

5. **見落としている仮説はないか。** 上の表で潰した10個以外に、これらの実測値（レンダラ<1GB、V8ヒープ余裕、GPU資源わずか100MB、ドローコール15,000、シーン64,000オブジェクト）と整合する原因の候補は？

6. **スマホ（iOS想定・ホーム画面Webアプリ）での固定メモリ上限に収める方針。** PCは再起動やメモリ増設という逃げ道があるが、スマホには無い。

---

## 9. 再現手順

1. `https://chronodrift.onrender.com/` を開く
2. 東京（新宿・東京駅周辺など超高密度エリア）へジャンプ
3. **何も操作せず放置**
4. 2〜4分でタブが落ちる（メッセージ無し）

計測が必要な場合は DevTools のコンソールを開き、`[mem]`（2秒ごと）と `[gpuBytes]`（10秒ごと）を見る。ただし DevTools を開くとログ保持で条件が変わるため、クラッシュ再現時は閉じておくのが望ましい。スマホでは 🩺 ボタンで画面左上に `[mem]` を表示できる。

## 10. 参考ファイル

- `js/legacy/part9.js` — `[mem]` / `[gpuBytes]` 計測器、`killGL()` / `freezeWorld()` / `freezeRender()`、animate ループ
- `js/legacy/part1.js` — レンダラ生成（`GFX` / `gfxPixelRatio`）、建物の距離アンロード、`dormantGrid`、道路メッシュキュー
- `js/legacy/part2.js` — `sharedFacadeBoxGeo`（建物ジオメトリ共有）、ファサードテクスチャのキャッシュ
- `js/legacy/part3.js` — 建物・道路の生成
- `js/legacy/part8.js` — OSMタイル取得と `pendingBuildings` への投入
- `server/server.js` — プロキシ・ディスクキャッシュ（OOM修正済み）
- `handoff-crash-not-three-data.md` — 本件の前段の調査記録
