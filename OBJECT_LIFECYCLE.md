# オブジェクト種類ごとの破棄・再生成の整理（2026-07-28時点）

「何がいつ捨てられ、戻ってきたときに復活するのか」を種類ごとにまとめたもの。
**恒久破棄**＝そのセッション中は二度と復活しない、の意味。

---

## 1. 一覧

| 種類 | 上限 | 破棄条件 | 恒久か | 再生成の経路 |
|---|---|---|---|---|
| **実建物（OSM）** | `PERF.bMax`（lite 6,000 / std 12,000 / high 25,000） | `unloadFarBuildings`：`bUnloadReal`（2,000〜4,000m）超 | 一時的 | `dormantBuildings` へ退避 →`reactivateNearbyDormantBuildings` が接近を検知して復帰 |
| **dormant建物（記述子）** | `DORMANT_MAX` = 60,000 | ①`DORMANT_KEEP_DIST`（≥4,000m）超のセルを丸ごと ②上限超過分を遠いセルから | **一時的（2026-07-28に変更）** | 捨てたタイルに `stale` 印 →`reviveStaleTiles` が5,000m以内で残存レコードごと破棄してタイル再取得 |
| **手続き生成建物** | チャンク単位 | `updateChunks`：`chunkR` 外のチャンクごと | 一時的 | `loadedChunks` から削除されるのでチャンク再生成で復活 |
| **道路・線路（メッシュ）** | — | `unloadFarRoads`：`ROAD_UNLOAD_DIST`（1,600〜3,200m）超。細街路は `minorRoadDist` | 一時的 | レコードは残るので `queueRoadMesh` で作り直し |
| **道路・線路（レコード）** | `ROAD_RECORD_SOFT_MIN` 20,000未満なら走査しない | `evictFarRoads`：`ROAD_RECORD_KEEP_DIST`（6,000m）超。高架は `MOTORWAY_UNLOAD_DIST × 1.3`（約13,000m）超 | **一時的（2026-07-28に変更）** | そのタイルの道路が**全滅**したら `dropTileRemnants` でタイルを再取得可能に戻す |
| **地形チャンク** | `chunkR` 範囲 | 範囲外 | 一時的 | 再接近で再生成（`loadedChunks` から削除される） |
| **遠景地形（WIDE/FAR）** | 1枚のグリッド | プレイヤー追従で作り直し | 一時的 | `checkWideTerrain` / `checkNearTerrain` |
| **面ポリゴンのメッシュ**（公園・水面・田畑・キャンパス） | `areaPolyBudget`：park 400 / water 400 / farm 250 / campus 400 | `unloadFarAreaPolys`：距離 | 一時的（距離ぶんは） | 再接近で作り直し |
| **面ポリゴンの予算** | 同上 | **セッション累計で使い切る** | **恒久** | **無し**（以降は回避判定とミニマップ表示のみ） |
| **土地利用ポリゴン**（`landusePolygons` / `avoidPolygons`） | 無し | 破棄されない | — | — （溜まり続ける） |
| **小物のインスタンスプール**（街灯・信号・ガードレール・ベンチ・看板・電柱・低木・街路樹） | プールごと（例：lampP 700、signalP 300、scrubP 2,400、guardP 2,500、benchP 400、樹木 `TREE_MAX` 30,000） | **セッション累計で使い切る。移動してもリサイクルされない** | **恒久** | **無し**（後から訪れた地域だけ小物が無い状態になる） |
| **森の木** | `forestTrunkP` 2,600 ほか | 移動のたび `rebuildForest` で作り直し | 一時的 | 唯一リサイクルされるプール |
| **ファサードマテリアル＋テクスチャ** | `facadeCache`（組み合わせ数ぶん） | 参照カウント0で `dispose`。canvasも0×0にして即返却 | 一時的 | 必要になれば再生成 |
| **建物ジオメトリ** | `sharedFacadeBoxGeo` のキャッシュ（実測259種） | 破棄しない（共有） | — | — |
| **標高タイル（GSI）** | `GSI_TILE_CACHE_MAX` 120枚（約31MB） | 古い順に破棄 | 一時的 | 再取得（HTTPキャッシュが効く） |
| **駅** | 無し | 破棄されない | — | OSMノードIDで重複排除（2026-07-28に駅名から変更）。距離破棄されないので再取得時の取り消しは不要 |
| **建物リレーション** | 無し | — | **一時的（2026-07-28に変更）** | `tileRelations` でタイル単位に取り消す → 再取得で復活 |

---

## 2. 2026-07-28に「恒久」から「一時的」へ変えたもの

以前は、距離アンロードでレコードを捨てても `queuedTiles` / `roadReadyTiles` が残るため、そのタイルは二度と再取得されなかった。**「タイルは緑（取得済み）なのに道路も建物も無い」が永続する**状態になっていた。

これを直すには取得済みフラグを落とすだけでは足りない。

- `seenOSMWays`（処理済みway ID）が残っていると、再取得しても全way が素通りされる
- かといってway IDだけ消すと、生き残っているレコードの上に同じ道路がもう1本生成される

そこで **「そのタイルの残存レコードを全部消す」と「フラグとway IDを落とす」を必ずセットで（原子的に）行う** 方式にした。

```
dropTileRemnants(タイルキー)
  ├ そのタイルの dormant セルを全部削除
  ├ way ID を seenOSMWays から取り消す（tileWays に記録してある）
  ├ relation ID を seenOSMRelations から取り消す（tileRelations に記録してある）
  ├ queuedTiles / roadReadyTiles / buildingReadyTiles / buildingQueuedTiles を削除
  └ gaveUp / 失敗回数 / backoff / timeoutBoost をクリア
        → 「一度も取得していない」状態に戻り、通常のタイル取得が拾い直す
```

呼び出し口は2つ。

| きっかけ | 条件 | 動作 |
|---|---|---|
| `evictFarRoads`（6,000m超） | そのタイルの道路が**全滅**した時だけ | 即座に再取得可能へ戻す。1本でも残っていたら何もしない（二重生成防止） |
| `reviveStaleTiles`（1.5秒ごと） | 建物だけ捨てたタイルに `stale` 印を付けておき、**5,000m以内**に戻ってきたら | 残っている道路レコードごと破棄してから、まるごと再取得 |

2つ目が必要な理由：密集地では dormant が上限60,000に張り付き、**2秒あたり1〜2万件**が捨てられている。そのタイルは道路が生き残るので「全滅判定」に掛からず、「道路はあるのに建物だけ永久に無い」が固定されてしまう。

再生成が走ると次のログが出る。

```
[staleTile] Nタイルを作り直します(道路レコードM本を破棄して再取得)
```

**距離5,000mの根拠**：道路メッシュの表示距離（`ROAD_UNLOAD_DIST` 最大3,200m）より外側で行う。手前でやると目の前の道路が一度消えて再生成され、ちらつく。

---

## 3. 起動直後のガード（`worldPosSettled`）

`animate()` は起動ブートストラップIIFEより**前**に呼ばれ、`checkOSMTiles()` はそこから回り始める。一方プレイヤーの開始位置が確定するのは `await startLocP`（位置情報取得）→ `jumpToLatLon` の後。**この数秒の窓では、距離判定の基準になるプレイヤー位置がまだ既定値**。

この窓で距離アンロードが走ると、これから居る場所のデータを恒久破棄してしまう。そこで `worldPosSettled` が立つまで、距離を根拠にした処理を全部止める。

対象：`unloadFarRoads` / `evictFarRoads` / `unloadFarBuildings` / `reactivateNearbyDormantBuildings` / `evictFarDormant` / `unloadFarAreaPolys` / part8.js の投入時振り分け

**安全網**：位置情報の許可が降りない場合 `await startLocP` が長時間解決しないことが実機で確認されている（Chromeはプロンプトを複数回無視されるとブロックする）。旗が立たないとアンロードが全部止まって際限なく積み上がるため、**15秒経ったら強制的に確定扱い**にする。

---

## 4. 恒久破棄だったもの（2026-07-28にすべて解消）

### (a) 小物のインスタンスプール ← 対応済み
街灯・信号・ガードレール・ベンチ・看板・電柱・低木・街路樹は、**セッション累計で使い切りで、移動してもリサイクルされなかった。** 一度上限に達すると、以降どこへ行っても二度と生成されない状態だった。

実機ログで実際に枯渇していたもの：
```
プール"(unnamed)"が上限(250)に達しました    ← addRoad から呼ばれる小物
プール"lampP"が上限(700)に達しました
プール"signalP"が上限(300)に達しました
プール"scrubP"が上限(2400)に達しました
プール"(unnamed)"が上限(30000)に達しました  ← 街路樹（TREE_MAX）
```
**対応**：`compactPools`（part2.js、3秒ごと）を追加。インスタンス行列から座標が読めるので、プレイヤーから遠いインスタンスを前詰めで捨て、空いた枠を新しい土地に回す。

- **発火条件**：9割以上埋まっているプールだけ。通常のプレイでは何もしない
- **保持距離**：`PROP_KEEP_DIST` = `max(2500, ROAD_UNLOAD_DIST)`。道路メッシュの表示距離以上なので、見えている範囲の小物は消えない
- **戻ってきた時**：そのタイルが再取得対象（`reviveStaleTiles`）になれば `addRoad` / `handleAreaFeature` が再実行され、小物も一緒に作り直される
- **森は対象外**（`noCompact`）。`rebuildForest` が移動のたび作り直すので二重管理になるため

回収が走ると `[poolCompact] Nプールから遠方の小物M件を回収しました(保持○○m)` が出る。

**注意**：`poolAdd` の戻り値（インスタンス番号）を呼び出し元が保持していると前詰めでズレる。現状 `poolSetY` に呼び出し元は無いので安全だが、今後インスタンス番号を覚える実装を足す場合はそのプールに `noCompact = true` を付けること。

### (b) 面ポリゴンの予算 ← 対応済み
`park 400 / water 400 / farm 250 / campus 400` を**セッション累計**で使い切ると、以降は回避判定とミニマップ表示だけになり3Dの面が出なくなっていた（実機ログで4種とも枯渇を確認）。

**対応**：予算の意味を「累計の使い切り」から「**今生きている面メッシュの数**」へ変更。`areaPolyBudgetOK()` が確保した種別を entry に引き継ぎ（`entry.areaKind`）、`dropAreaRecordsInTile` で落とす時に `areaPolyRefund()` で返す。あわせて、細すぎるポリゴンで `buildTerrainFollowingAreaPoly` が諦める際に予算が返らず垂れ流しになっていた既存の漏れも塞いだ。

### (c) 再取得でスキップされるもの ← 2026-07-28に対応済み
- **building relation（マルチポリゴンの建物・スタジアム等）**：`seenOSMRelations` をタイル単位（`tileRelations`）で取り消すようにした。これが無いと、タイルを再取得しても `synthesizeBuildingRelationWays` が合成をスキップし、relation由来の建物だけ永久に欠けていた。
- **駅**：`seenStations` の重複判定を「駅**名**」から「OSMノードID」に変更した。以前は同名駅（「本町」「大手町」「中央」など日本では珍しくない）が別の街にあっても2つ目以降が永久に生成されなかった。なお駅は距離で破棄されないため、タイル再取得時に `seenStations` を取り消す必要は無い（取り消すと逆に二重生成になる）。

### (d) 面ポリゴンの重複 ← 対応済み
`areaPolyMeshes` / `landusePolygons` / `avoidPolygons` は距離で破棄されないため、同じタイルを再取得すると記録が重複して積まれ、面メッシュが重なって z-fighting していた。

**対応**：`dropAreaRecordsInTile`（part4.js）を追加し、`dropTileRemnants` から呼ぶ。外接矩形の**中心**が属するタイルで判定するので、タイルをまたぐ大きなポリゴン（湾・大河川など）が二重に消えることも消え残ることも無い。空間ハッシュ（`areaPolyGrid` / `avoidGrid` / `landuseGrid`）は生存分から作り直す（道路の `roadGrid` と同じ方針）。

---

## 5. 現在の状態

**OSM地図に存在するデータ（道路・建物・建物リレーション・駅）は、恒久破棄されない。** すべてタイル単位で再取得できる。

装飾側（小物プール・面ポリゴン予算）も、累計の使い切りではなく「今生きている数」で管理する方式になり、移動し続けても枯渇しない。

残っている制約：

| 項目 | 内容 |
|---|---|
| 再取得のコスト | タイルを作り直すとOverpassへ再問い合わせが走る。サーバーのディスクキャッシュとIndexedDBが効くので実負荷は小さいが、ゼロではない |
| 小物の復活タイミング | `compactPools` が回収した小物は、そのタイルが再取得されるまで戻らない（保持距離2,500m以上なので見えている範囲では起きない） |
| インスタンス番号 | `compactPools` は前詰めするため、`poolAdd` の戻り値を保持する実装を足す場合はそのプールを `noCompact` にする必要がある |
