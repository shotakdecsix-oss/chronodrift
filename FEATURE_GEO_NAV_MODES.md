# 位置情報・ルート系 新機能 設計メモ

> このドキュメントはアイデア出し・設計検討チャットのまとめ。実装はまだ着手していない。
> 対象は既存の3D探索アプリ(ChronoDrift系、現行の`index.html`+`js/legacy/part1〜9.js`)。
> `GAME_DESIGN.md`(文明育成シミュレーション構想)とは別軸の、現行アプリへの機能追加。

## 0. 前提(現状把握)

- 現在の「現在地」ボタン(`geoBtn`, part7.js)は `getCurrentPosition` を**1回だけ**呼んでジャンプするのみ。継続追従はしていない。
- 地名検索でのジャンプ機能(`mapSearchBtn` / `searchPlaceJump`)は既にある。出発地・目的地入力UIの土台として流用できる。
- プレイヤー移動は常時WASD操作(仮想歩行)。実座標との対応は `xzToLatLon` / `latLonToXZ` で変換。
- 線路データ(`railway`タイプの線分セグメント)と駅ノード座標(`globalStationPoints`、`railway=station/halt`)は**既存のOverpassクエリで取得済み**(part1.js, part2.js)。新規通信は不要。

## 1. 4つのモードの全体像

| モード | 内容 | 依存関係 |
|---|---|---|
| A. GPS実移動同期 | 実際に外を歩くとゲーム内アバターも追従(watchPositionで継続追従) | 独立 |
| B. 経路シミュレーション | 出発地・目的地入力→経路検索→その経路をゲームが自動再生 | 独立(「経路に沿ってカメラを自動追従させるエンジン」を新規構築) |
| 電車でGOモード | 線路上で発動→線路をロック→一人称視点で電車スピードのオート移動 | Bの自動追従エンジンを再利用(経路ソースが検索結果→線路データに変わるだけ) |
| C. A+B組み合わせ(ナビ表示) | GPS実移動に追従しつつ、目的地までの経路を画面表示 | A・電車 完成後に着手 |

**実装優先順位:A → B → 電車でGO → C**(ユーザー指定)。

## 2. A: GPS実移動同期

**目的**:実際にスマホを持って外を歩くと、ゲーム内プレイヤーもリアルタイムでその通りに動く(Ingress/ポケモンGO的)。

**方針**:既存の`geoBtn`(1回きりの`getCurrentPosition`)を、`watchPosition`による継続取得に拡張。WASD自由モードとは切り替え式で共存。

**詰めるべき論点**:
- **向き(heading)の取得**:GPSの緯度経度だけでは向きが取れない。Device Orientation API(コンパス)を使う場合、iOSは`requestPermission()`のユーザー操作起点の許可ダイアログが必要。移動ベクトルからの推定(静止時はコンパス、移動中はGPS移動方向)との併用も検討。
- **GPS誤差とスムージング**:市街地で数十m単位のジッターが出るため、平均化などのノイズ対策が必要。
- **建物との衝突判定**:GPS誤差で建物にめり込む可能性がある。GPSモード中は水平衝突判定を緩和する、または道路上へのスナップ処理が必要。
- **スケール対応**:既存の`xzToLatLon`変換が1:1対応である前提の確認。
- **電池・通信負荷**:高精度`watchPosition`+常時3D描画+移動につれた新規タイル取得が重なる。更新頻度の調整が必要(既存の現在地タイルの並列度・timeout改善が下地としてある)。

**残課題**:向き取得方式の確定、衝突判定の扱いの確定、更新頻度の具体値。

## 3. B: 経路シミュレーション

**目的**:出発地・目的地を入力すると経路検索し、その経路をゲームが自動再生する(実際に歩かなくてもその道のりを3Dで疑似体験できる)。

**ルーティング方式**:**車移動(driving)で統一**する方針に確定。OSRM公開デモサーバー(`router.project-osrm.org`)の`driving`プロファイルをそのまま利用でき、APIキー登録・自前ホストが不要になり実装コストが大きく下がる。

**車ルート統一で割り切った点(検討済み・許容)**:
- 歩行者専用エリア(神社の参道、商店街のアーケード、公園内の小道、川沿いの遊歩道など)は通れない。名所の魅力が薄れる場合がある。
- 都市部の高架・トンネル区間(首都高など)を通ると、地上の街並みから切り離された区間や真っ暗な区間が発生し、景観の没入感が途切れる可能性がある。
- 一方通行やインターチェンジ構造で、実距離以上に遠回りするルートになる場合がある。
- → 将来的に名所周辺だけ徒歩プロファイル(openrouteservice等)に差し替える拡張余地は残しておく。v1では対応しない。

**詰めるべき論点**:
- 出発地・目的地の入力UI(既存の地名検索`mapSearchBtn`/`searchPlaceJump`を2点指定に拡張して流用)。
- 自動再生の速度、早送り・一時停止操作の有無。
- 長距離経路(例:東京→大阪)でのタイル生成負荷対策(距離上限を設けるか、逐次生成に任せるか)。
- 経路を3D空間上にライン表示するか、カメラを沿わせるだけにするか。
- 途中で一時停止して自由に寄り道(WASD自由モードへ切替)できるようにするか。

**残課題**:速度の具体値、距離上限値、UI詳細。

## 4. 電車でGOモード

**目的**:線路上で機能を発動すると、線路をロックして一人称視点のまま電車スピードでオート移動する。

**データ**:線路線分・駅ノードは既存Overpassクエリで取得済み(§0参照)。新規通信は不要。ただし**駅データは今回考慮しない**(発着点として使わない)。

**発動・出発点**:発動地点から**一番近い線路タイル(セグメント)**を出発点とする。駅の有無は無視してよい。

**視点**:新規の運転台視点は作らない。**既存の一人称視点をそのまま流用**する。

**分岐の扱い**:**直進優先**(現在の進行方向との角度差が最小の候補セグメントを自動選択)。理由:
- プレイヤー入力なしで乗車体験が途切れず没入感を保てる。
- 分岐点での判断は実際の運転士のように事前に必要で、リアルタイムのプレイヤー操作では反応速度的に厳しい。
- 極端な急カーブでない限り、実際の路線形状ともだいたい一致する。
- 注意点:OSMの複線区間(上り線・下り線が並走する別ウェイ)で誤って隣のトラックに乗り換えないよう、セグメント接続判定の許容距離は数m程度に絞る。

**詰めるべき論点**:
- 線路セグメントの接続グラフ構築方法(端点マッチング。全体グラフを事前構築するのではなく、タイルストリーミングに合わせて局所的にその都度たどる形で足りるはず)。
- 速度プロファイル(一定速度か、加減速を再現するか)。
- 終了条件(任意タイミングで途中下車できるようにするか、自動終了のみにするか)。

**残課題**:速度の具体値、終了条件のUI、接続判定の許容距離の具体値。

**アーキテクチャメモ**:このモードは実質的に「Bで作る"経路に沿ってカメラを自動追従させるエンジン"を、経路ソースを検索結果から線路データに差し替えたもの」。速度プロファイルが電車用になり、視点は一人称固定(視点自体はBと共通)。Bのエンジンを先に作っておくと電車モードの実装コストが下がる。

## 5. C: A+B組み合わせ(ナビ表示)

**目的**:GPS実移動(A)に追従しつつ、目的地までの経路を画面上に表示するナビゲーション機能。

**前提**:A・電車でGOの基盤ができてから着手。

**詰めるべき論点(概略のみ、A/電車完成後に詳細化)**:
- ナビ表示の形式(進行方向の矢印か、経路全体のライン表示か)。
- ルートから外れた時に再検索するか、表示だけで放置するか。
- 目的地到達の判定距離。

## 6. まとめ:実装順序と理由

1. **A(GPS実移動同期)** — ユーザー優先指定。向き取得・GPS誤差対応など現実世界のノイズと向き合う基盤を先に作る。
2. **B(経路シミュレーション)** — 「経路に沿ってカメラを自動追従させるエンジン」を新規構築。車ルート統一でOSRM公開サーバーがそのまま使え、実装コストは比較的低い。
3. **電車でGOモード** — Bのエンジンを線路データに転用。データは取得済みのため新規実装は接続グラフ+分岐ロジック+速度プロファイルが中心。
4. **C(組み合わせ)** — A・電車の基盤の上に、ナビ表示機能を追加。

> 次のステップ:このチャットは設計・アイデア出しまで。実装に入る際は本ドキュメントを起点に、Aの向き取得方式から詳細設計する。
