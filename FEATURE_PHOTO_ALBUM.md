# 機能設計: 写真アルバム(スマホ内の写真を実座標にマップする)

作成: 2026-08-03 / ステータス: **フェーズ1 設計確定・未実装**

---

## 1. 目的

スマホ内の写真を EXIF の GPS 情報で実座標に対応づけ、ゲーム世界の**その場所に空中フォトパネルとして立てる**。
自分が実際に撮った写真が、実際に撮った場所に浮かんでいる状態を作る。

---

## 2. 確定仕様(ユーザー判断済み)

| 論点 | 決定 |
|---|---|
| 表示形態 | **空中に浮かぶ写真パネル**(看板型・遠方からも視認できる) |
| 取り込み | 初回に**まとめて一括選択** → 以降は自動復元(一枚ずつ選ばせない) |
| 表示範囲 | **近傍のみ**。ただし**ジャンプマップ上では全写真の位置が分かる** |
| 端末 | iPhone (iOS Safari) |
| フェーズ2 | ゲーム内カメラ(撮影 → 即その場に設置)。**今回は対象外** |

---

## 3. 技術前提(実機検証済み)

`exif-test.html` にて 2026-08-03 に iPhone 実機で検証:

- 「写真ライブラリから選ぶ」(`accept="image/*"`) → **GPS 取得成功**
- 「ファイルとして選ぶ」(accept なし) → **GPS 取得成功**

→ iOS Safari が位置情報 EXIF を剥がす懸念は**空振り**。企画は成立する。

ライブラリは `exifr@7.1.3`(UMD, CDN)。`exifr.gps(File)` が File/Blob を直接受けられるため、
ArrayBuffer に展開せずに済む(メモリ的に重要)。

---

## 4. 越えられない制約と、その回避策

### 制約: Web に「写真ライブラリへのアクセス許可」API は存在しない

ブラウザは**ユーザーが明示的に渡したファイルしか見えない**。カメラロールを監視・自動取り込みする
手段は Safari / Chrome ともに無く、これは仕様上の意図的な制限。ネイティブラップ(Capacitor 等)なら
可能だが別プロジェクト級のため今回は取らない。

### 回避策 A: 一度だけ選ばせて永続化する

初回に一括選択 → **緯度経度 + サムネイルを IndexedDB に保存** → 2回目以降は起動時に自動復元。
体感は「最初の1回だけ許可して、あとは勝手に出る」になる。

### 回避策 B: 重複排除で「全部選び直し」を安全にする

`id = ハッシュ(ファイル名 | サイズ | 撮影日時)` で取り込み済み判定。
**毎回まとめて全部選び直しても既存分は無視され、新規分だけが追加される。**
iOS のピッカーは新しい順に並ぶので、実際には先頭の数枚を選ぶだけで更新できる。

### 回避策 C: 更新のうながし

前回取り込みから 7 日以上経過していたら、ジャンプマップに「写真を更新」バッジを出す。

---

## 5. データモデル

IndexedDB `photoAlbum` (version 1) — 既存の `osmTileCache` とは**別 DB**。

### store `photos` (keyPath: `id`)

| フィールド | 型 | 内容 |
|---|---|---|
| `id` | string | `name|size|DateTimeOriginal` の簡易ハッシュ |
| `lat` / `lon` | number | EXIF GPS |
| `alt` | number\|null | GPSAltitude(あれば) |
| `taken` | number | 撮影日時 epoch ms(無ければファイル lastModified) |
| `w` / `h` | number | サムネの実ピクセル寸法(アスペクト比の算出用) |
| `thumb` | Blob | JPEG, 長辺 512px, quality 0.72 |
| `added` | number | 取り込み日時 |

### store `meta` (keyPath なし / key 直指定)

- `lastImportAt`: number

### 容量試算

512px JPEG ≈ 40KB。500 枚で約 20MB。iOS Safari の IndexedDB は数百MB 使えるため実用上問題ない。
**安全弁として保存上限 2000 枚**(超過時は古いものから削除するのではなく、取り込みを止めて警告)。

### なぜサムネが 512px なのか

- 空中パネルは近距離で見上げるため 256px では粗い
- 512×512 以下ならテクスチャの GPU コストは軽い
- パネルタップ時の拡大表示にもそのまま流用できる(元画像は保存しない)

---

## 6. 取り込みフロー

1. HUD の 🖼 ボタン、またはジャンプマップ内の「写真を更新」から起動
2. hidden な `<input type="file" accept="image/*" multiple>` を click
3. 選択されたファイルを**逐次処理**(同時実行 3 まで)
   1. `exifr.gps(file)` → GPS 無しならスキップ(カウントのみ)
   2. `id` 算出 → 既存なら**デコードすらせず**スキップ
   3. `createImageBitmap(file)` → canvas で長辺 512px に縮小 → `toBlob('image/jpeg', 0.72)`
   4. `ImageBitmap.close()` を**必ず**呼ぶ(iOS でメモリを食う)
   5. IndexedDB に put
4. 進捗表示: `123 / 400 枚処理中(GPS付き 87 / 新規追加 12)`
5. 完了後、近傍パネルを再構築 + `lastImportAt` 更新

**注意**: iOS では HEIC がそのまま渡ることがある。exifr も Safari の `createImageBitmap` も
HEIC を扱えるため対応不要だが、デコードは重い。同時実行を 3 に抑える理由。

---

## 7. ワールド配置

### 座標

写真は **lat/lon を正とし、XZ は都度 `latLonToXZ()` で算出**する。
`recenterOrigin()`(part4.js:186)でジャンプのたびに `MID_LAT`/`MID_LON` が変わるため、
**XZ をキャッシュしてはいけない**。ジャンプ時は全パネルを破棄して新原点で組み直す。

### 表示制御

| パラメータ | 既定値 | 内容 |
|---|---|---|
| `PHOTO_VIEW_DIST` | 600m | この距離以内の写真だけ実体化 |
| `PHOTO_MAX` | 60 | 同時表示上限(超過分は近い順に採用) |
| 再評価トリガ | プレイヤーが 50m 移動 | 毎フレーム評価はしない |

### パネルの構成

- `PlaneGeometry` + テクスチャ。**写真のアスペクト比を保持**
- 幅 `PHOTO_PANEL_W = 8m`、高さは w/h から算出
- 白フチ: ひと回り大きい白い Plane を背面に配置(2枚構成)
- 支柱: 地面から浮遊高さまで細い柱。看板らしさが出る
- 浮遊高さ: `getGroundY(x, z) + 12m` (part5.js:106)
- ビルボード: **Y 軸のみ**カメラに追従(yaw only)。上下には傾けない
- 遠距離視認: 距離に応じてスケールを緩く補正(最大 2.5 倍まで拡大)。遠くの写真が豆粒にならないように
- 撮影日はサムネ生成時に Canvas へ焼き込む(実行時の文字描画コストをゼロにする)

### 地形との整合

支柱の根本は `getGroundY()` にスナップする。NEAR 地形の更新で地面高さが変わると浮くため、
`trackResnapInstance`(part1.js:888)と同じ思想で**再スナップ対象リストを自前で保持**する。
プール方式ではなく個体レコードを持つため、既存の `resnapPropsAndTreesInBounds` には相乗りしない。

> 参考: 2026-08-02 の「ジャンプ後に木・信号機が浮く」不具合と同じ罠。最初から作り込むこと。

### 大原則との整合(重要)

MEMORY の「**地図データ忠実性を最優先する大原則**」に対し、フォトパネルは実データではない**上乗せ要素**。

**建物・道路の生成予算を一切奪わないこと。** 専用の管理配列で独立させ、`bMax` 等の
既存カウンタには含めない。チャンクキューにも積まない。写真パネルのせいで
実データの生成が遅れる、という事態は絶対に避ける。

### テクスチャ管理

- Blob → ImageBitmap のデコードはパネル生成時に**非同期・遅延実行**。同時デコード上限 2
- デコード完了までは灰色のプレースホルダ枠を出す
- パネル破棄時に `texture.dispose()`。Geometry は共有して使い回す

---

## 8. ジャンプマップ表示

`openMapJump()`(part7.js:192)にフックする。Leaflet は既に導入済み。

- 全写真を `L.marker` で表示。アイコンはサムネを丸く切った `divIcon`(48px)
- **近接クラスタリング**: ズームレベルに応じてグリッド集約し、複数なら「📷 12」の集約アイコンにする
  (数百枚をそのまま置くと地図が埋まる)
- マーカータップ → `jumpToLatLon(写真の座標)` でその場所へジャンプ
- 写真レイヤーの **ON/OFF トグル**を用意
- マップ下部に「写真を更新」ボタン。前回取り込みから 7 日以上でバッジ付き

---

## 9. パネルのタップ拡大(フェーズ1に含める)

パネルをタップ → 画面中央にサムネを大きく表示 + 撮影日時 + 「ここへジャンプ」。
元画像は保存していないため 512px サムネの拡大表示になる(サムネを 512px にした理由のひとつ)。

---

## 10. 設定

PERF プリセット(⚙)には**乗せない**。写真は実データ生成とは独立させたいため、専用トグルを持つ。

- 写真パネル ON / OFF
- 表示距離 300 / 600 / 1200m
- 「取り込み済み写真をすべて削除」

---

## 11. 実装ファイル

| ファイル | 変更 |
|---|---|
| `js/legacy/part11.js` | **新規**。写真アルバム機能一式 |
| `index.html` | exifr の script タグ、part11.js の読み込み(part10 の後)、HUD ボタン、進捗UI、拡大表示オーバーレイ、CSS |
| `js/legacy/part7.js` | `openMapJump()` に写真マーカー描画のフック 1 行 |
| `js/legacy/part9.js` | `animate()` にビルボード更新のフック 1 行 |
| `js/legacy/part1.js` | i18n キー追加(`t()` は part1.js:237) |

---

## 12. フェーズ2(今回対象外)

**ゲーム内カメラ**: HUD に 📷 を置き、`<input type="file" accept="image/*" capture="environment">` で
その場撮影 → GPS 追従モード(モードA)の現在地をそのまま座標に使い、撮った瞬間にパネルが立つ。
EXIF に依存せず、撮影後の写真が完全自動で反映される。「歩いて撮って世界に残す」という
体験としてはこちらが本命になりうる。
