以下は前のチャットからの引き継ぎ内容です。この続きから対応してください。

---

## 目的

three.js製のブラウザゲーム **ChronoDrift**(`C:\Users\Shoichi\Desktop\isehara-game`、Renderにpushで自動デプロイ)で、
**OSMの水域(川・海)を地図データに忠実に描画する**こと。

このプロジェクトの大原則は「**マップデータの忠実な表現を、手続き生成やパフォーマンスより常に最優先する**」。
水面まわりはこの原則に何度も立ち返りながら、2週間ほど直し続けている領域。

私(前チャットのClaude)の役割は**分析と指示書の作成のみ**で、実装はユーザー側(下位者)が行う。
コードは私が書き換えていない。

---

## これまでの経緯・決定事項

### ユーザーの進め方の好み

- 会話は日本語。トークン消費は最小限に。
- **実装は下位者に任せる。私は分析と指示書(`IMPL_PROMPT_*.md`)を作る。**
- コード変更後は必ず `git add -A; git commit; git push` のコマンドを添える(PowerShellは古いので `;` 区切り)。
- **PC ChromeのDevToolsが使える。**「Macが無いからライブデバッグ不可」は誤解で、
  本番URLをPCブラウザで開けばコンソールで実測できる。**推測を重ねる前に必ず実測する。**

### 到達した設計判断(`DESIGN_20260804_WATER.md`)

**「標高データ(DEM)には陸の高さだけを聞き、どこが水か・水面が何mかは一切聞かない」**

- 水域の範囲 = **OSMポリゴン**(実測輪郭)。DEMには聞かない
- 水面の高さ = **岸(陸)のDEM値** または **海面固定値**。水域内部のDEM値は使わない
  (DEMは陸の製品で、水域上の値は測定されていない=提供側が空洞や内挿で埋めた捏造値)
- 地形は水域ポリゴンに譲る(輪郭で切る)
- 感潮域(tidal)の水面は定義上どこでも海面
- 閉じた水域の水面 = 縁の最も低い点(流出口)= **min**。max ではない

### 積み上がった大原則(一部)

- 「今はやらない」を「やらなくてよい」に変換しない(保留フラグを消さない)
- 保留を作るなら回収経路と滞留計器を同じコミットで入れる
- 欠測(null)を `|| 0` で既定値に潰さない
- 最大値と累積伝播を組み合わせない(1点のノイズを全域に配る増幅器になる)
- 近似(構築物)を形状判断の入力に使わない。実測輪郭のみ
- 2つの面を「わずかな差」で重ねる設計にしない
- 毎回作り直すBufferGeometryは全属性を同時に作り直す(`normal`だけ使い回すと黒くなる)

---

## 現在の状況

**壊れている。ロールバックを決めた。**

直近の実機確認で、ユーザーから「**AとCの悪化。前回まで水面だった部分も地面になっている**」と報告:

- **A. 見た目** — 水面だった部分が地面になる、陸が水面になる、地面が黒く消える
- **C. 性能** — タイル取得待ち54秒、取得キュー140件、GPU推定7.4GB、
  建物が毎秒1万件規模で生成と差し戻しを空回り(`generated/2s 15006 requeued/2s 17770`)

### ロールバック方針(未実施。新チャットで最初にやること)

**`e98f5a8`(2026-08-04 `sea: raise fixed sea Y offset...`)の状態まで `js/legacy/` を戻す。**

この時点は「水面が浮いたり沈んだりはするが、黒い穴も陸の水面化も起きていない」既知の状態。
それ以降(08-05〜08-12)の全コミットは水面/coastline関連なので、`js/legacy/` を丸ごと戻して問題ない。

```
cd C:\Users\Shoichi\Desktop\isehara-game
git branch water-experiment-20260812
git checkout e98f5a8 -- js/legacy/
git commit -m "revert: water rendering back to 2026-08-04 (e98f5a8) as a known-usable baseline"
git push
```

`git branch` で現状を保存しておくこと(捨てるのではなく退避)。
`IMPL_PROMPT_*.md` / `DESIGN_*.md` は残す(調査記録として価値がある)。

---

## 次にやること

1. **上記ロールバックを実行し、実機(PC + スマホ)で「黒い穴・陸の水面化が消えたこと」を確認する。**
   ここが土台。ここが確認できるまで次に進まない。
2. 性能(C)の切り分け。水面とは別クラスの問題:
   - タイル取得待ち54秒 / 取得キュー140件
   - GPU推定7.4GB(クラッシュ域)
   - 建物の空回り(`requeued/2s 17770`)
   - `pendWater` が123まで跳ねる(水域ポリゴンの回収待ち滞留)
   - `kind=lake` が異常に多い(`bins=1` が何百件)。river/sea 以外を全部 lake に分類している疑い
3. 水面(A)は、土台が安定してから**1つずつ、単独で検証できる単位で**入れ直す。

---

## 試して駄目だったこと(繰り返さないため)

### 設計として失敗が確定したもの

- **地形に穴を開ける(terrain hole)** — 200mセル単位では岸線が最大200mずれる。
  境界セルの陸パッチを入れたが、そこが黒くなる/陸が消えるを連発した。
- **Sutherland-Hodgman の半平面逐次交差でセルを切る** — クリップ側が凸である前提が必要。
  川の輪郭は非凸なので陸パッチが空になり黒い穴になる。
  **同じ誤りを海岸線リボン v3→v4 で一度潰しており、2回目**。
- **coastline由来のポリゴン(Phase1 ribbon / Phase2 タイル全塗り)を地形マスクに使う** —
  ribbonは「海岸線+3000mオフセット+クリップ」の構築物であって実測輪郭ではない。
  向き判定を1本外すとマンハッタン(島)の内側が海になる。実測座標で確認済み。
- **`side: DoubleSide` を巻き順ミスの保険にする** — `MeshLambertMaterial` は頂点シェーダで
  ライティングするので裏返った法線を救えない。症状を「見えない」から「黒い面」に変えるだけ。

### 私(Claude)の指示ミスで壊した箇所

- **min化(水位)と適合カットを別コミットに分けた** — 互いの前提を打ち消す関係で、
  片方だけの中間状態が成立しない。「川が大きな四角の集まり」の原因。
- **Phase1 ribbon はマスクしてよい**と判断した(実際は構築物なのでダメ)。
- **巻き順が裏返っている**と誤診(外積を計算すると +Y で正しかった。
  真因は `normal` 属性の使い回しによる範囲外書き込み)。

### 有効だった修正(ロールバック後も再適用の価値あり)

- **欠測(GSI 404)を `|| 0` で0mに潰していた** → 橋の水没6回失敗の真因。`terrainYOrNull` で区別。
- **`_dirty` を距離チェックで `false` に書き換えていた** → 橋の再構築要求が永久に握りつぶされる。
- **way帰属を「最初のノード」1タイルだけにしていた** → 線路・幹線が再取得で復活しない(otk化で修正)。
- **`_isNearCoverage` が `nearElev` の有無を見ておらず嘘をつく** → 診断が全部無駄になる。

---

## 調査資料(リポジトリ内)

- `DESIGN_20260804_WATER.md` — 水面の設計やり直し(考え方の本体)
- `IMPL_PROMPT_20260803_WATER_SURFACE.md` — 地形が200m格子である事実と1次元プロファイルの導出
- `IMPL_PROMPT_20260803_BRIDGE_WATER_v2.md` — 欠測を0に潰す問題(実測で確定)
- `IMPL_PROMPT_20260805_*.md` — 穴あけ・陸パッチ・マスク寿命・tidal・coastline の各修正指示
- `handoff-water-rendering.md` — このファイル

---

## 進め方について(前チャットからの反省)

水面まわりは**15ラウンドほど「直すたびに別の場所が壊れる」を繰り返した。**
原因は、変更が相互に前提を打ち消し合う関係にあるのに、
症状ベースで1つずつ潰そうとしたこと。

新チャットでは:

- **1変更 = 1コミット = 単独で検証できる単位**を守る。分割できないなら1コミットにまとめる。
- **推測で修正指示を出す前に、必ずPC ChromeのDevToolsで実測する。**
  この方針に切り替えてから、原因特定が一気に速くなった。
- 実測は「座標を出してGoogleマップと突き合わせる」まで含める。
  コード内の数字だけ見ていると、正しいものと壊れているものの区別がつかない。
