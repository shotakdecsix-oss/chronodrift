# 実装指示: 感潮域(tidal)の水位を海面に確定させる — NYで陸が水面になる真因

作成: 2026-08-05 / 実機ログ(NY)で確定。`IMPL_PROMPT_20260805_NY_LAND_AS_WATER.md`(クランプ除外)は
実装済みだが**それは原因ではなかった**。本命はこちら。

---

## 1. 実測(ログから)

`tidal=true` の水域の水位(ゲーム単位):

```
kind=lake  tidal=true   6.2 / 6.3 / 16.5 / 17.4 / 17.6 / 28.2 / 31.2 / 31.4 /
                        32.2 / 35.8 / 36.1 / 38.9 / 39.2 / 51.6
kind=river tidal=true   bins=17 max=12.4 / bins=35 max=19.1 / bins=21 max=33.0
```

**感潮域の水面は、どれも同じ「海面」でなければならない。**
6.2〜51.6 にばらついていること自体が、海面ではなく**岸サンプルから決めている証拠**。

ニューヨークの岸は岸壁・埠頭なので実標高で数m〜20m上。
そこに水面を置けば、当然まわりの陸を飲み込む = **「陸が水面になる」。**

相模川の内陸側は `tidal=false` なので影響を受けない。
**「相模川は改善したがNYは駄目」という差がそのまま裏付けになっている。**

---

## 2. 原因(コード)

`js/legacy/part4.js:568-573` `_computeWaterLevelFromBankInfo`

```js
if (tidal) {
  const mouthFloorY = seaLevelY() + RIVER_MOUTH_ABOVE_SEA_MARGIN;
  if (M0[mouthBin] < mouthFloorY) M0[mouthBin] = mouthFloorY;
}
```

問題が2つある。

1. **下限しか掛けていない。** 上限が無いので、岸サンプルが高ければ水位はいくらでも上がる。
2. **効くのは河口ビン1本だけ。** `kind=lake` は `bins=1` なので、
   この分岐が実質ほぼ何もしていない。

`DESIGN_20260804_WATER.md` 3-3 に「tidal なら河口ビンを海面にクランプ」とだけ書き、
**`lake` について書かなかった**こと、**「下限」としか書かなかった**ことが原因。設計書側の記述漏れ。

---

## 3. 修正

### 3-1. `tidal === true` なら、プロファイル全体を海面に確定する

物理的な定義そのもの: **感潮域の水面 = 海面。**

```js
// js/legacy/part4.js _computeWaterLevelFromBankInfo
// 【2026-08-05・NY実機】感潮域(海と繋がっている水域)の水面は、定義上どこでも海面。
// 岸サンプル(都市部では岸壁・埠頭で数m〜20m上)から決めてはいけない。
// 旧実装は「河口ビンを下から持ち上げる」下限クランプで、上限が無く、
// かつ bins=1 の lake には実質効いていなかった。
if (tidal) {
  const seaY = seaLevelY() + RIVER_MOUTH_ABOVE_SEA_MARGIN;
  for (let b = 0; b <= nBins; b++) M0[b] = seaY;   // ★ 全ビン。下限ではなく確定値
  // 以降のラチェット伝播・上向き平滑化は全ビン同値なので実質no-opになる(消さなくてよい)
}
```

- `kind=lake tidal=true` も `kind=river tidal=true` も**全部同じ高さに揃う**。
- **相模川内陸は `tidal=false` なので一切影響を受けない**(前回の改善を壊さない)。

### 3-2. `_isTidalNear` の判定を締める(必須・セットで入れる)

現在は海域から `TIDAL_SEA_PROXIMITY_M = 300` 以内で `tidal=true` になる(`part4.js:679`)。

3-1 を入れると **tidal 判定の誤検出がそのまま「水域が地面に埋もれて消える」に直結する。**
海岸から300mの内陸の池(標高20m)まで海面に下げてしまう。

**近接ではなく、実際の接触/重なりを条件にすること。**

```js
// bboxの近接ではなく、そのポリゴンがsea領域と実際に重なる/接するかで判定する
// (例: 自ポリゴンの輪郭点のいずれかが sea entry の内側にある、
//       または sea entry の輪郭点のいずれかが自ポリゴンの内側にある)
```

判定を厳しくする方向にだけ倒すこと。**tidal を取りこぼしても「水位が岸基準で高い」に戻るだけだが、
誤検出すると水域が消える。** 失敗した時の被害が小さい側へ寄せる。

---

## 4. 検証

**リロードしてから、NYと相模川の両方**を見る。

1. **NY**: 陸が水面になっていないこと
2. **NY**: ログの `tidal=true` の水位が**すべて同じ値**になっていること
   → これが一番確実な合格判定。ばらついていれば 3-1 が効いていない
3. **NY**: ハドソン川・湾が水面として正しく見えること(下がりすぎて消えていないこと)
4. **相模川**: 前回の改善が維持されていること(**デグレしていないこと**)

**この段階でも「浮き」は評価しない。** 暫定クランプがまだ効いている。

---

## 5. 別件(今回の症状とは無関係だが記録)

`pendWater`(回収待ち水域ポリゴン)が **8 → 11 → 26 と単調増加**している。

回収経路が追いついていないか、回収条件が永久に成立しないポリゴンが滞留している。
放置すると「その水域が永久に出ない」に育つ(このプロジェクトで4回踏んでいる型)。
**次の観測で減っているかを確認し、増え続けるようなら独立の課題として立てること。**

---

## 6. 大原則への追記

33. **「下限クランプ」と「確定値」を混同しない。**
    `if (x < floor) x = floor` は上振れを一切防がない。
    物理的に値が一意に決まる量(感潮域の水面=海面)は、
    **クランプではなく代入で確定させる。**

34. **判定の誤りが「消える」方向に効く場合は、判定を厳しい側に倒す。**
    tidal 判定は、取りこぼせば「水位が高い」に戻るだけだが、
    誤検出すると水域が地面に埋もれて消える。
    **失敗時の被害が非対称なら、被害の小さい側へ寄せる。**
