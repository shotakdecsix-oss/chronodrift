# 相談プロンプト: 川の水面が「正しい形」と「正しい高さ」を両立できない

以下をそのままFable 5に貼ってください。

---

three.js製のブラウザゲーム「ChronoDrift」(実世界の地図データをOSM/Overpass APIから取得し、リアルタイムに街を生成して歩き回れるゲーム)で、大きな川(神奈川県・相模川)の水面ポリゴンを何度直しても、「地形が水面に混ざり込む/水面が突き破られる」「水面が地形から浮いて見える」「川の輪郭が格子状にカクカクして不自然」のどれかに必ず落ち着いてしまい、6回連続で修正に失敗しました。新しい視点で根本設計を見直してほしいです。

## アプリの構成

- `index.html` + `js/legacy/part1.js`〜`part10.js`(元は1つの巨大なインラインスクリプトを機械的に分割したもの。グローバルスコープを共有する昔ながらの`<script>`タグの並び読み込みで、ES Modulesではない)
- OSM Overpass APIからタイル単位で道路・線路・建物・landuse・水域等を取得し、three.jsのメッシュを動的生成。
- `getGroundY(x, z)`(part6.js): 地形の高さを返す唯一の関数。実測の標高データ(NEAR/WIDE二段階の解像度グリッド、外部APIから取得)を補間して返す。**水中の実測データ(水底標高・バスシメトリ)は一切無い**。水域ポリゴンの内側でこの関数を呼んでも、返ってくるのは「もしそこに川が無かったら地表がどんな高さだったか」を周辺の陸地データから補間した、水面表現には本質的に不適切な値でしかない。これが全ての問題の根本にある制約。
- 「マップデータへの忠実性を、手続き生成やパフォーマンスより常に最優先する」ことをこのプロジェクトの大原則としている(何度かの根本原因調査でこの原則に立ち返って対症療法を退けてきた経緯がある)。川の輪郭はOSMの実測ポリゴン(way/multipolygon relation)なので、**形は絶対に実データ通りにしたい**。

## 水域の2つの表現経路

1. **面ポリゴン(実形状)**: `natural=water`/`waterway=riverbank`タグを持つOSMのway(`handleAreaFeature`、part4.js)またはmultipolygon relation(`processWaterRelation`、part4.js。相模川のような大河川はほぼ確実にこちら)から、実際の川岸の輪郭ポリゴンを取得し、面メッシュを張る。**これが本題。**
2. **推定幅リボン(フォールバック)**: `waterway=river`等の中心線タグから、`width`/`est_width`タグ(無ければriver=16m等のデフォルト)で推定した幅の帯(道路と同じ`addRoad`経由のメッシュ)を描く。実形状より明らかに不正確。面ポリゴンが下記の理由で描画に失敗すると、代わりにこのリボンしか見えない状態になる。

## これまでの修正の変遷(全部同じ関数`_instantiateAreaPolyMesh`/`buildAreaPoly`まわり、part4.js)

### 症状1: 「相模川がずっとリボンのまま」→ 2つの原因を発見・修正済み

1. **予算切れの永久ロスト**: 面メッシュのドローコール予算(`areaPolyBudget.water`、セッション中の存命エントリ数で管理)を使い切ると、そのポリゴンは二度と再試行されず永久に消えていた。→ 予算切れ時に記述子を`pendingAreaWaterPolys`キューへ退避し、低頻度スキャンで予算に空きができたら再構築する仕組みを追加。
2. **relation版の重複排除が独立していてun-seeされない**: `processWaterRelation`は建物のmultipolygon relationとは別の、独自の重複排除Set(`seenOSMRels`)を使っていた。一方、面メッシュの破棄自体は位置ベース(そのポリゴンの外接矩形の中心が属するタイルが再取得対象になると破棄される)で、相模川のように広いbboxを持つrelationは、プレイヤーから遠く離れた無関係なタイルがstaleになるだけで日常的に破棄されうる。破棄されても`seenOSMRels`は解除されないので二度と再構築されなかった。→ 建物relationと共通の`seenOSMRelations`(タイル帰属+un-seeの仕組みが既にある)に統一。

この2つで「リボンのまま」は解消(ユーザー確認済み)。ただし、面ポリゴンが実際に描画されるようになったことで、**次の症状が可視化された**。

### 症状2: 「川面に正しくない地面描写が混ざりこんでいる」

水面の面メッシュ構築(`buildAreaPoly`、`entry.kind==='flat'`)は、`THREE.ShapeGeometry`でOSMの輪郭点(`pts`。広い川では`thinPts`で間引き済み、頂点間隔が数十〜数百m)をそのまま三角形分割し、**各頂点ごとに**`getGroundY(x,z) + yOff`で高さを付けていた。水中の実測データが無いため、間隔の広い頂点でサンプルした値は周辺の堤防・中州・岸の起伏がそのまま漏れ込み、頂点間の大きな三角形がその起伏をなぞって「地面が混ざり込む」ように見えていた。

### 修正1: 輪郭頂点の下位25%平均を一律適用 → 「直っていない」

輪郭点だけから下位25%点の平均高さを計算し、ポリゴン全体に一律適用。**ユーザーに「まだ直っていない」と再指摘され、調査の結果、別の独立した関数`rebuildAreaPolyMesh`(NEAR地形更新のたびに呼ばれ、面メッシュの高さを再スナップする既存の仕組み)が、無条件に「頂点ごとgetGroundY追従」の古いロジックのままだったと判明**。1箇所だけ直しても、プレイヤーが近づいて地形が更新されるたびにもう1箇所が古い計算で上書きし戻していた。

### 修正2: 高さ計算を共有関数化 + ポリゴン内部を含む格子の最大値を一律適用 → 「水面が空中に浮いている」

`_instantiateAreaPolyMesh`(新規構築)と`rebuildAreaPolyMesh`(NEAR地形更新時)の両方が同じ関数を呼ぶよう統一。輪郭だけでなくポリゴン内部も格子状(40mセル、`buildTerrainFollowingAreaPoly`と同じ手法)にサンプルし、その**最大値**+yOffを一律適用(=地形が水面を突き破ることを構造的に無くす狙い)。**しかし川のように長く緩やかな高低差のあるポリゴンでは、一番高い場所に全体の水面を合わせることになり、他の(低い)場所で水面が地面から大きく浮いて見える結果になった。**

### 修正3(直近・今回も失敗): グローバル1値をやめ、格子分割+局所最小値スムージングの頂点追従に変更 → 「直っていないし、川の形が四角になっている」

グローバルな1値では「浮き」と「突き抜け」が原理的に両立しないと判断し、方式を変更。水面メッシュを、輪郭点だけのShapeGeometryから、`buildTerrainFollowingAreaPoly`と同じ**格子分割**(40mセル、境界セルは4隅全てがポリゴン内側の格子点のみ張る)に切り替え、各格子点の高さは生の`getGroundY`ではなく`_smoothGroundMinWater(x,z)`(半径20mの十字4点+中心の`getGroundY`の最小値。孤立した1点だけのノイズを吸収しつつ広域の緩やかな傾斜には追従する意図)を使うようにした。holesは`pointInPolygon`で格子点ごとに除外(ShapeGeometryのネイティブhole機能を失った代替)。

```js
// part4.js _instantiateAreaPolyMesh、entry.kind==='flat'分岐(水面専用)
const WATER_SMOOTH_R = 20;
function _smoothGroundMinWater(x, z) {
  const r = WATER_SMOOTH_R;
  return Math.min(
    getGroundY(x, z),
    getGroundY(x + r, z), getGroundY(x - r, z),
    getGroundY(x, z + r), getGroundY(x, z - r)
  );
}
// 格子分割(cellSize=40、最大80×80セル、buildTerrainFollowingAreaPolyと同じ手法):
// 4隅の格子点全てが pointInPolygon(pts) && !inAnyHole(holes) を満たすセルだけ三角形を張る。
// 各格子点の頂点: verts.push(x, _smoothGroundMinWater(x, z) + yOff, z);
```

**結果**: ユーザーから「直っていないし、川の形が四角になっていてリアル感がない」と報告。高さの問題(浮き/突き抜け)がまだ残っている可能性に加え、**格子分割そのものが新しい問題を生んだ**——格子は軸に沿った正方形セル単位でしか輪郭を近似できないため、OSMの実測ポリゴンが持つ滑らかな川岸のカーブが失われ、階段状の四角い輪郭になってしまった(`buildTerrainFollowingAreaPoly`のコメントには元々「輪郭がわずかに内側へ痩せるが、実用上は問題ない簡易対応」と書かれており、校庭・田畑のような小さく規則的な形状では気にならなかった簡易対応が、長く曲がりくねった川では致命的に目立った)。

## 気づいていること(まだ試していない方向性)

修正2・3を通して「形(輪郭)」と「高さ」を同じ格子解像度で同時に扱おうとしたことが問題を難しくしていた可能性がある。**輪郭の形状(ShapeGeometryによる正確な三角形分割)と、高さのサンプリング(ノイズを均すための空間的な処理)は、本来別々に扱えるはず**——輪郭は元のOSM座標(`pts`)をそのまま使って正確な三角形分割をしつつ、各頂点の高さだけ`_smoothGroundMinWater`のような空間的スムージングを適用する、という組み合わせを一度も試していない。ただしこれで本当に解決するかは未検証で、他にも見落としがあるかもしれない。

## 知りたいこと

1. 「形状の忠実性」(OSM輪郭をそのまま使う)と「高さの安定性」(浮きも突き抜けも起こさない)を両立する設計として、上記の「輪郭はそのまま・高さだけ空間スムージング」は妥当か。他に見落としているアプローチはあるか。
2. 水中に実測データが無い(`getGroundY`が水面表現に本質的に不適切な値を返す)という制約の中で、「地形が絶対に水面を突き破らない」を保証しつつ「不自然に浮いて見えない」も両立させる、確実な高さの決め方はあるか(例: 川沿いの実測標高から水位を推定する、輪郭に近いほど信頼するグラデーション方式にする、等)。
3. 大きな川(相模川)は複数のOSMタイルにまたがるmultipolygon relationとして届き、`stitchRings`で複数wayを縫い合わせて閉じたリングを作っている。この縫い合わせ自体(輪郭点の順序・重複除去)が不正確で、輪郭がそもそも歪んでいる可能性はあるか。切り分け方法があれば知りたい。
4. そもそも三角形分割の方式(`THREE.ShapeGeometry`によるearcut)自体が、複雑な実形状の大河川(中州・支流合流点等を含む)に対して不向きで、別の三角形分割ライブラリ・アプローチに切り替えるべきという判断はあるか。

Macが無く実機のライブデバッグができないため、コードレビュー・設計レビューベースでの助言が中心になります。よろしくお願いします。
