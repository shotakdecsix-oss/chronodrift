# 水面の設計やり直し — DEMに「水」を聞くのをやめる

作成: 2026-08-04 / これまでの一連の水面バグ(浮く・沈む・地面になる・橋が水没する)の設計的な決着。
個別の修正指示ではなく、**何をどのデータから導くか**の再定義。

---

## 0. 一行で

**標高データ(DEM)には「陸の高さ」だけを聞き、「どこが水か」「水面が何mか」は一切聞かない。**

これまでの修正はすべて「水面の高さをDEMからどう賢く導くか」の系統だった。
だから全部同じ形(浮く⇔沈む)で失敗した。データが持っていない情報を絞り出そうとしていたため。

---

## 1. 原則: どのデータに何を聞くか

| 知りたいこと | 正しい情報源 | 聞いてはいけない情報源 |
|---|---|---|
| どこが水域か(平面形状) | **OSMポリゴン**(実測輪郭) | DEM |
| 水面の標高 | **岸(陸)のDEM値** / **海面固定値** | **水域内部のDEM値** |
| 陸の高さ | DEM | — |
| 岸線の位置 | **OSMポリゴンの輪郭** | DEMの等高線・格子 |

### なぜ水域内部のDEMを使ってはいけないか

DEMは陸の地表を測る製品で、水面上の値は測定されていない。提供側が
空洞(void)にするか、内挿・定数で埋めている。**測定値ではなく誰かが作った値。**
さらに本プロジェクトはその上に3層の加工を重ねている:

- 欠測 → `|| 0`(ゲーム高さ0 = `elevBase` 相当に化ける)
- 欠測 → `oceanFloor` センチネル
- 実測値 → `Math.max(m, landFloorM)` で0.5m以上に底上げ

`_computeWaterProfile` が数字を受け取る時点で、
**「実測0.4mの河岸」と「データが無いので発明した0.5m」が区別できない。**
橋の水没(2026-08-03)の真因はこれだった。

### 解像度の前提

- 地形メッシュ = **200m格子**(元データはNEAR約540m / WIDE約1km)
- 川幅 = 30〜500m、岸線 = **幅ゼロの線**

**200m格子で100m幅の川や岸線は原理的に表現できない。**
形はOSMから取り、DEMには高さだけを担当させる、という分業以外に解はない。

---

## 2. データモデル: 「水域(water body)」を一級の実体にする

現状は「水面ポリゴン(描画物)」しか存在しない。高さの決定に必要な情報が
ポリゴンごとにバラバラに再計算され、種別も曖昧なままだった。

```js
// 水域エンティティ(1つの物理的な水域 = 1つ)
{
  id,                    // OSM way/relation id
  kind,                  // 'sea' | 'river' | 'lake'
  rings,                 // 外周(複数可) + holes(中州・島)。OSM実測輪郭そのもの
  tidal,                 // 海に接続しているか(bool)
  level,                 // 水面高さ。'lake'/'sea' はスカラー、'river' は1次元プロファイル
  levelSource,           // 'sea-fixed' | 'bank-min' | 'pending' — 診断用に必ず持つ
  meshes,                // 描画用(GPU解放・再構築の単位)
}
```

**種別の判定(OSMタグから。DEMは見ない)**

| kind | 判定 |
|---|---|
| `sea` | `natural=coastline` 由来の塗り、または `water=bay/strait/lagoon/harbour` |
| `lake` | 閉じた単一リングで、`water=lake/pond/reservoir/basin` |
| `river` | `waterway=riverbank`、`water=river`、または細長い(bbox長辺/短辺が大きい)水域 |

`tidal` = その水域の輪郭が **sea領域(既存の `areaKind==='sea'` エントリ)と交差または近接**するか。
判定は既にある `areaPolyGrid` の照会で足りる。

---

## 3. 水面高さの決定規則

**共通の前提: 岸のDEM値だけを使う。** サンプル点は輪郭から**外向き法線に20〜50m出した点**
(= 実測された陸)。水域内部の格子ノードは一切使わない。
現行の `NODE_MARGIN`(輪郭の外側100mを内側と混ぜて拾う)は廃止する。

### 3-1. `sea` → 実標高0m固定

既に正しく動いている(実測 `海(固定Y) 実標高0.65m`)。
**ただし `seaYOffset()`(= `LAND_FLOOR_MARGIN_M*ELEV_SCALE + 0.3`)は廃止し、真の0mに戻す。**
これは「地形が `landFloorM` で底上げされて海面より高くなる」ことへの対処だったが、
4章(地形カット)で地形が水域から退くので不要になる。**修正がハックを1つ消す。**

### 3-2. `lake` → 岸の**最小値**

閉じた水域の水面は、その縁の**最も低い点(=流出口)の高さ**に等しい。
物理的に正しい推定量は **min**(現行の max/90%点ではない)。

```
level = percentile(bankSamples, 10%)      // 純粋なminは1点のノイズに弱いので低位パーセンタイル
level = max(level, seaLevel)              // 海面より低い内陸湖は無い(閉塞湖は対象外とする)
```

### 3-3. `river` → 流下方向1次元プロファイル、**各断面は両岸の低い側**

```
1) 主軸(PCA第1主成分。簡易版はbbox長辺)に沿って BIN=200m で分割
2) 各ビン: そのビンに属する岸サンプルの低位パーセンタイル(10%点) = L[b]
3) tidal なら、海に最も近い側の端ビンを seaLevel に**クランプ**する
4) 河口→上流へ単調非減少になるよう伝播。1ビンあたりの上昇量は MAX_RISE_PER_BIN で制限
5) 移動平均で平滑化(単調性を壊さない範囲で)
```

**maxではなくminを使えるようになる点が今回の設計の要。**
これまで max を使っていたのは「地形が水面を突き破らない」ことを幾何学的に保証するためだった。
4章で地形を輪郭で切れば**その要求自体が消える**ので、物理的に正しい推定量に戻せる。
「浮く」という失敗モードが構造的に無くなる。

### 3-4. データが揃わないとき

岸サンプルが1点も取れない(地形未到達)なら `levelSource='pending'` として
**描画せず、必ず回収キューに積む**。ここは 2026-08-04 に一度踏んでいる
(「保留したまま誰も拾わず、水域が永久に消える」)。**回収経路と滞留計器(`pendWater`)を同じコミットで入れる。**

---

## 4. 地形が水域に譲る(適合カット)

**200mセル単位の穴あけでは岸線が最大200m(対角283m)ずれる。** 大原則に照らして不合格。
境界セルだけ、水域輪郭で**切る**。

```
セル(200m正方形)と水域ポリゴンの関係で3分岐:
  (a) 完全に水域の内側 → farMesh から穴あけ。パッチ無し
  (b) 完全に外側       → 従来どおり2三角形
  (c) 境界にかかる     → 穴あけ + 「陸側パッチ」を別メッシュで描く
        c-1. セル矩形を水域ポリゴンでクリップし、陸側の多角形を得る
        c-2. earcut で三角形化
        c-3. 各頂点の高さは farSurfaceY(x,z) で取る
```

**c-3 が最重要。** `farSurfaceY`(part5.js)はそのセルの2三角形が張る面の解析式なので、
そこから取った高さは必ず元の面の上に乗る。**「描画される面 = `getGroundY` が返す値」という
本プロジェクト唯一の健全な不変条件が保たれる。** ここを別式にすると足元・建物・道路が全部狂う。

- 対象は境界セルのみ(NYの湾でも数百セル)。水マスクが変わった時だけ再構築。
- クリップは「凸多角形(セル矩形)を任意多角形で切る」だけ。三角形化は水面用の earcut がそのまま使える。
- 岸線がOSM輪郭ちょうどになり、**高さの大小に依存しなくなる。**

**残る制約(明示しておくこと)**: 岸の「形」は忠実になるが、岸の「高さ」の解像度は
200m格子のまま。段差のある護岸は再現されない。**形と高さは別問題として切り分ける。**

---

## 5. 高さの単一情報源

```js
getGroundY(x, z)   // = farSurfaceY。「陸の地表」。水域内部でも面の解析値を返す(切っても式は不変)
waterLevelAt(x, z) // その点を含む水域の水面高さ。無ければ null
surfaceY(x, z)     // = waterLevelAt ?? getGroundY を優先した「乗れる面」
```

- 生成物の接地(建物・木・道路)は従来どおり `getGroundY`
- **プレイヤー・当たり判定・橋のクリアランス判定は `surfaceY` / `waterLevelAt`**
- 橋は現行の `waterSurfaceYAt` をそのまま `waterLevelAt` に置き換えるだけでよい
  (way単位で最大値を取る方式は `IMPL_PROMPT_20260803_BRIDGE_WATER_v2.md` 修正Dのまま維持)

---

## 6. 廃止するもの

この設計に移ると、以下は**存在理由が消える**。残すと必ず再発源になるので消すこと。

| 対象 | 廃止理由 |
|---|---|
| 水域内部の格子ノード収集 | DEMに水を聞かない |
| `NODE_MARGIN`(外側100mを内側と混ぜる) | 岸サンプルを正式な入力にするため不要 |
| `_binPercentileMax` の max方向 | min方向に反転 |
| `WATER_MARGIN`(突き抜け防止の嵩上げ) | 地形が退くので不要。視認用の微小値のみ残す |
| `seaYOffset()` | `landFloorM` との競合が消える。真の0mへ |
| `_waterYAt` の profile==null フォールバック | 保留は描画しない方針に統一 |
| `_isNearCoverage` の嘘(`nearElev` 未確認) | 計器が嘘をつくと推論が全部無駄になる。修正して残す |

---

## 7. 実装順(必ずこの順。各段で実機確認)

| # | 内容 | 確認方法 | 規模 |
|---|---|---|---|
| 1 | 水域エンティティ化 + 種別判定 + `tidal` 判定 | プローブで `kind`/`tidal`/`levelSource` を一覧表示 | 中 |
| 2 | 岸サンプルによる高さ決定(3章)。**描画は現行のまま** | ハドソンの `level` が**実標高0m付近**になること | 中 |
| 3 | 適合カット(4章) | 岸線がOSM輪郭と一致。空が見える穴が無いこと | 中 |
| 4 | `WATER_MARGIN` / `seaYOffset` を撤去、`surfaceY` 差し替え | 水面が浮かない。プレイヤーが沈まない | 小 |
| 5 | 6章の廃止コードを削除 | `[water] profile UNAVAILABLE` が出ないこと | 小 |

**2 が単独で検証できるのが重要。** 描画を変えずに「水位の数字が正しくなったか」だけを
先に確かめられる。ここで数字が合わなければ、3以降に進んでも必ず失敗する。

---

## 8. 落とし穴(既に踏んだもの)

1. **`farSurfaceY` の三角形分割と自前インデックスの向きを一致させる。** 崩すと全部狂う。
2. **保留を作ったら回収経路と滞留計器を同じコミットで入れる。**
   gaveUpタイル・`_dirty`・`_vegCleanupQueue`・`waterProfile=null` と4回同じ失敗をしている。
3. **キャッシュした派生値には無効化トリガーを対で設計する。**(`waterProfile` の凍結)
4. **近似ポリゴンを幾何学的な判断の入力に使わない。**
   coastline Phase2 のタイル全塗りを地形マスクに流用して陸が消えた。
   **描画に使ってよい近似と、形状判断に使ってよい実測を、型で分ける。**
5. **原点付け替え(近距離ジャンプ)でマスク・キューを必ずパージする。**
6. **計器自体の正しさを検証対象に含める。**(`near=yes` が嘘をついていた)

---

## 9. 大原則への追記

23. **各データソースに「答えられる質問」だけを聞く。**
    DEMは「陸の高さ」を答えられるが、「どこが水か」「水面が何mか」は答えられない。
    答えられない質問をして返ってきた値は、欠測でも推定でもなく**捏造**である。
    パイプラインの入口で、どのソースに何を聞くかを明示的に決める。

24. **幾何学的な防御(max・マージン)が必要になったら、
    それは「2つの表現が同じ空間を奪い合っている」というサイン。**
    防御量の調整に入る前に、どちらかを退かせられないかを先に考える。
    退かせられれば、物理的に正しい素直な式に戻せる。

25. **近似と実測を型で区別する。**
    同じ「ポリゴン」という形をしていても、
    実測輪郭(OSM)と近似(タイル全塗り)は別の型として扱い、
    形状判断のAPIは実測しか受け付けないようにする。
