# 改善アイデア 深掘り(2026-08-19)— 8項目

> 実装は別チャットに委譲する前提。各項目は「**現状のコード実測** → **本当の課題** → **段階案** → **触るファイル/勘所** → **完了判定**」の順。
> 調査対象:`index.html`(39KB)、`js/legacy/part1〜10.js`、`js/core/mode-registry.js`。

## ⚠ 最初に:前回メモの誤りを2つ訂正

前回のアイデア出しはドキュメントベースだったため、**既に実装済みのものを「新規提案」として挙げてしまっていた**。コードを読んで判明:

| 前回の提案 | 実際 |
|---|---|
| A-1「現在地の地名を表示」 | **すでに実装済み**。`part7.js:19-59` に Nominatim 逆ジオコーディングによる住所表示(`#addressDisplay`)があり、約10秒 かつ 150m 移動ごとに更新している。国コード判定(建物スタイル用)も相乗りしている |
| B-1「時刻連動」(前回の最優先) | **部分的に実装済み**。`#timeMorningBtn / timeNoonBtn / timeEveningBtn / timeNightBtn / timeAutoBtn` が既にあり、`applyTimeOfDay` が fog 濃度も含めて切り替えている |

→ 以下の A-1 は「**住所の次に何を出すか**」として組み直した。過去の教訓([[feedback_search_existing_mechanism_before_reimplementing]])どおり、着手前の grep は必須。

---

# A-1. 現在地の「地名」を常時表示する(→ 再定義:**近くの"名前のある場所"を出す**)

## 現状(実測)

- `part7.js:19-59` `updateAddressDisplay()` / `checkAddressDisplay()`
  - Nominatim `/reverse?zoom=18&addressdetails=1&accept-language=ja` を叩き、`city|town|village|county` + `suburb|neighbourhood|quarter|city_district` を連結して `📍 伊勢原市石田` のように表示。
  - 更新条件は **600フレームごと かつ 前回位置から150m以上移動**。BAN回避のため意図的に絞っている(コメントに明記あり)。
  - 座標を `toFixed(4)`(約11m)に丸めてプロキシのディスクキャッシュに当てている。
- 一方、**Overpassで取得しているタグに `place=*` も `historic=*` も `tourism=*` も無い**。実測した全クエリ:
  ```
  way["highway"] / way["building"] / relation["building"]
  way["railway"="rail"] / node["railway"="station"] / node["railway"="halt"]
  node["public_transport"="station"]
  way["waterway"~"river|stream|canal|riverbank"] / relation["waterway"="riverbank"]
  way["natural"~"water|wood"] / way["natural"="coastline"] / relation["natural"="water"]
  way["leisure"~"park|garden|playground"]
  way["landuse"~"residential|commercial|industrial|retail|mixed_use|farmland|orchard|meadow|allotments|forest"]
  way["amenity"~"school|university|college|hospital"]
  ```
- 建物の `name` タグは `LANDMARK_TOWER_NAMES`(`part2.js:1091`)で**完全一致の名前検出**にだけ使われている(東京タワー等4施設)。それ以外の名前は捨てている。
- **3D空間内にテキストを出す仕組みは存在しない**(`Sprite` の使用箇所ゼロ。`CanvasTexture` は空・建物テクスチャ用のみ)。

## 本当の課題

「地名が出ない」ではなく、**「目の前にある建物・施設が何なのか分からない」**。伊勢原を歩いていて大山阿夫利神社の鳥居の前に立っても、画面には `📍 伊勢原市大山` としか出ない。世界に固有名詞がゼロなので、実在の場所を歩いている実感が住所表示1行で頭打ちになっている。

## 段階案

**Phase 1(最小・推奨)— 駅名だけ出す**
- `globalStationPoints`(`railway=station/halt` ノード、**既に取得済み**)に `name` タグが乗っているはず。まずそこだけ確認。
- プレイヤーから半径200m以内に駅があれば、住所表示の下に `🚉 伊勢原駅 (120m)` と1行足す。
- 通信ゼロ・新規クエリゼロ。実装は近傍探索1つとDOM 1行。**最も安いのにインパクトが大きい**。

**Phase 2 — name付きの実建物を拾う**
- 既存の `way["building"]` クエリのレスポンスには `tags.name` が既に含まれている(捨てているだけ)。
- 建物メッシュ生成時に `name` があれば `{x, z, name, kind}` を軽量な配列+空間ハッシュに登録(`landuseGrid` と同じ `polyGridAdd/queryPolyGrid` の仕組みを流用)。
- 半径50m以内で一番近いものを住所行の下に表示。`🏛 伊勢原市役所` のように。
- **通信は一切増えない。** これが本命。

**Phase 3 — Overpassクエリに名所を足す**
- `node|way["historic"]`、`node|way["tourism"~"attraction|museum|viewpoint"]`、`way["amenity"~"place_of_worship"]` を追加。
- ⚠ 注意:クエリを増やすとタイル取得が重くなる。過去に道路タイル停滞で散々苦労している領域なので、**独立した低優先度クエリ**として、道路・建物が終わってから投げる形にする(既存の隔離キュー/dormant の考え方を流用)。ここだけは慎重に。

**Phase 4(任意)— 3D空間内のラベル**
- `THREE.Sprite` + `CanvasTexture` で名前を空中に浮かべる。ただし数が増えるとドローコールが効くので、**同時表示は最大5個・距離順**などの明示的な上限を最初から設計に入れる([[feedback_per_building_decoration_budget]] の教訓)。

## 触るファイル / 勘所

- `part7.js`(住所表示のすぐ下に追記)、`part2.js`(建物タグのパース箇所)、`part1.js`(空間ハッシュ)。
- 勘所:**Phase 1〜2 は通信ゼロで完結する**。Phase 3 で初めて通信が増える。Phase 2 までを1回、Phase 3 を別回、と分けて出すのが安全。

## 規模 / 完了判定

- Phase1: **小**。判定 = 伊勢原駅から300m以内を歩くと駅名が出る。
- Phase2: **小〜中**。判定 = 市役所・学校・コンビニ等の前で名前が出る、かつタイル生成のFPS・レイテンシに変化がない。
- Phase3: **中**+リスク。判定 = 名所名が出る、かつ **道路タイルの生成レイテンシが悪化しない**(悪化したら即revert)。

---

# A-8. 写真アルバムと散歩の統合

## 現状(実測)

- **アルバム機能そのものが未実装**。`exif|EXIF|album|Album` の文字列が `js/` にも `index.html` にも1件も無い。
- あるのは設計書 `FEATURE_PHOTO_ALBUM.md`(10KB)と、動作検証用の単独ファイル `exif-test.html`(5.7KB)だけ。前回メモで「設計確定・別チャット委譲済み」となっているが、**コードには入っていない**。

→ したがって A-8(散歩との統合)は**A-8単独では着手できない**。まず「アルバム本体」が要る。

## 分解

**A-8a. アルバム本体(前提)**
- ユーザーが写真を選択 → EXIF の GPS を読む → `latLonToXZ` でワールド座標へ → その地点に板ポリゴン(またはピン)を置く。
- 保存は IndexedDB(既にOSMキャッシュで使っている。**キャッシュキーは必ず絶対座標ベースにする** — 過去に原点相対キーで都市間衝突を起こしている[[project_isehara_game_dense_city_jump_diagnosis]])。
- Web にはライブラリ権限APIが無いので**自動取り込みは不可能**。ユーザーが `<input type="file" multiple>` で明示的に選ぶ形が唯一の道(設計書にも記載済み)。

**A-8b. 散歩との統合(本題)**
ここが「他の誰も持っていない体験」になる部分。具体的には3つ:

1. **近接通知** — 歩いていて写真の撮影地点から100m以内に入ったら、HUDに `📷 この先に3枚の思い出` と出す。A-1 Phase1 とまったく同じ近傍探索の仕組みで作れる。
2. **空中の写真** — 撮影地点に写真が浮かんでいて、近づくと大きく表示される。撮影方位が EXIF にあれば(`GPSImgDirection`)、その方向を向いた板にする。無ければカメラ正対。
3. **ミニマップに撮影地点のドット** — 一番安い。ミニマップ描画(`part7.js`)にレイヤーを1枚足すだけ。

**A-8c. GPS追従モードとの掛け算(ここが本当の価値)**
実際にその場所に行くと、その場所の思い出が出てくる。GPS追従モードは既に実機動作確認済みなので、**「アルバム × GPS追従」だけで完結した1つの商品になりうる**。IDEA_WORLD_CANVAS.md が「③は多人数が揃わなくても単独で価値が完結する」と書いているのはまさにこれ。

## 勘所 / 注意

- **プライバシー**:写真+位置は最もセンシティブな組み合わせ。**サーバーに一切送らない(全部ブラウザ内で完結)**を最初から仕様として明記する。これは実装をむしろ簡単にする(サーバー改修ゼロ)。
- **EXIFパース**:`exif-test.html` が既にあるので、そこで検証済みのコードをそのまま持ち込む。外部ライブラリを足す場合は、Renderで配信している都合上 CDN 依存を増やしすぎないこと。
- **枚数の上限**:100枚を超えると板ポリゴンとテクスチャでメモリを食う。テクスチャは長辺512pxにリサイズしてから保存、同時に scene に出すのは近い順20枚まで、といった上限を最初に決める。

## 規模 / 完了判定

- A-8a: **中**(1〜2チャット)。判定 = 手持ちの写真10枚を読み込んで、正しい場所にピンが立つ。
- A-8b: **小**(A-8a の後なら)。判定 = 歩いて近づくと通知が出る/写真が見える。
- A-8c: **小**。判定 = GPS追従で実際に外を歩き、思い出の場所で写真が出る。

---

# B-2. 遠景の山影

## 現状(実測)— 想定と違った

| 項目 | 実測値 | 場所 |
|---|---|---|
| 地形メッシュ `farMesh` の大きさ | `FAR_SIZE = 12000`(**半径6km**)、`FAR_SEGS = 60` → 200m格子 | `part5.js:37-49` |
| 広域標高データ `wideElev` の範囲 | `WIDE_HALF_LAT/LON = 0.13` → **±約14km**、`WIDE_SEGS = 28` → 約1km間隔 | `part6.js:30-33` |
| カメラの far | **5000**(spaceのみ5800) | `part1.js:469` |
| フォグ | `FogExp2`, density = **0.00056**(明治0.0004 / 宇宙0.00008) | `part1.js:461` |

**つまり地形データもメッシュも既に十分遠くまである。山が見えない原因はフォグ。**

FogExp2 の減衰は `exp(-(density·d)²)`:

| 距離 | 残る割合(density 0.00056) |
|---|---|
| 1,000 m | 73% |
| 2,000 m | 29% |
| 2,500 m | 15% |
| 3,000 m | 6% |
| 4,000 m | 0.6% |
| 5,000 m | **0.04%(実質完全に消える)** |

大山は伊勢原駅から約7〜8km、丹沢主稜はさらに奥。**現在の設定では、そもそも大山は「メッシュの外(6km超)」であり、仮にメッシュがあってもフォグで完全に消えている**。富士山(約60km)は論外。

## 本当の課題

「遠景メッシュを作る」ではなく、**「遠景を見せるための3点セット(メッシュ範囲・カメラfar・フォグ)を一貫して設計する」**。そして重要なのは、**フォグは近景のポップイン(生成の遅れ)を隠すために意図的に濃くされている**こと(`part1.js:459` のコメントに明記)。単純にフォグを薄くすると、建物が虚空から湧く様子が丸見えになる。

## 段階案

**Phase 1 — 「山影レイヤー」を別メッシュで足す(推奨)**
- 既存の `farMesh`(6km・フォグあり)はそのまま。
- **新規に「horizon mesh」を1枚**追加:半径 6km〜25km を覆う低解像度リング(`WIDE_SEGS` 相当の1km格子、頂点数は数百程度)。
- このメッシュだけ **`material.fog = false`**、色は空の色に寄せた単色〜2色グラデ(青灰色のシルエット)、`depthWrite: false`、`renderOrder` を空(-2)と地形(0)の間に置く。
- これで「近景はフォグで守られたまま、地平線に山のシルエットだけが浮かぶ」が両立する。
- 標高データは `wideElev`(±14km)を流用。それより外は不足するので、25kmまで伸ばすなら `WIDE_HALF_LAT/LON` を広げるか、**もっと粗い third tier(±60km、5km格子)を1回だけ取得**する。富士山を出したいなら後者が必要。

**Phase 2 — カメラ far を伸ばす**
- 山影メッシュは far の外に置けないので、`camera.far` を 5000 → 30000 程度に。
- ⚠ **深度バッファ精度が犠牲になる**。near=0.5, far=30000 の比は 60000:1。既に水面と地形の z-fighting に `polygonOffset` で対処している(`part5.js:25-28`)ので、**far を伸ばすと z-fighting が再発するリスクが高い**。
- 対策:`renderer` が `logarithmicDepthBuffer: true` に対応できるか確認する(モバイル対応と性能に注意)。あるいは **near を 0.5 → 1.0 に上げる**だけでも精度は倍になる(一人称視点なら実害はほぼ無い)。
- ここは**この項目で一番危ない部分**。「水面の往復10回」を再演しないため、**Phase 1 を単独でデプロイして山影が出ることを確認してから** Phase 2 に触ること。

**Phase 3(任意)— 大気遠近**
- 距離に応じてシルエットの色を空の色へブレンドすると、一気に「らしく」なる。頂点カラーで距離を焼き込むだけなのでシェーダ改造は不要。

## 触るファイル / 勘所

- `part5.js`(farMesh の隣に horizonMesh を新設)、`part6.js`(wideElev の範囲、必要なら third tier の取得)、`part1.js`(camera.far / fog)。
- 勘所:**farMesh には一切手を入れない**。水面・地形・海岸線の合意事項が全部 `farNodeY` に集約されているので、そこを触ると水面問題が再燃する。**純粋に加算する新メッシュ**として作ること。

## 規模 / 完了判定

- Phase1: **中**。判定 = 伊勢原の平地から北西を向いたとき、大山〜丹沢のシルエットが見える。
- Phase2: **中**+リスク高。判定 = 山影が見える かつ **水面/橋/道路の z-fighting が発生していない**(ここは実機で必ず確認)。
- Phase3: **小**。

---

# B-3. 地面を landuse で塗り分ける

## 現状(実測)

- **landuse ポリゴンは既に取得・保持されている**:`landusePolygons`(`part1.js:1928`)+ 空間ハッシュ `landuseGrid`、参照は `landuseTypeAt(x,z)`(`part1.js:973`)。
- 取得タグ:`residential | commercial | industrial | retail | mixed_use | farmland | orchard | meadow | allotments | forest`。
- **ただし用途は「建物の見た目を決めること」だけ**:
  - `part3.js:44` — 商業/工業区画ならオフィス/商業ビル寄りの外観にする
  - `part8.js` — 手続き生成の可否判定(`buildable()`)
  - `part4.js:1501` — `isFarm` 判定
- **地面の色は完全に標高だけで決まっている**:`terrainColorRGB(h)`(`part5.js:146-157`)。低地の緑 → 山地の深緑 → 岩 → 雪、というグラデーション。landuse は一切参照していない。
- 地形マテリアルは `MeshLambertMaterial({ vertexColors: true })`(`part5.js:25`)なので、**頂点カラーを差し替える口は既に開いている**。

## 本当の課題

現在の地形は「標高が同じなら色も同じ」。伊勢原の平野は住宅地も水田も畑も**まったく同じ緑1色**。上から見ても歩いても、地面が情報を持っていない。

一方で難しいのは、**地形メッシュの格子が200m(`FAR_STEP`)しかない**こと。1つの三角形が200m四方あるので、そこに細かい landuse の境界を頂点カラーで表現するのは無理がある(田んぼ1枚は50m程度)。

## 段階案(2つの道があり、選択が必要)

**案A:頂点カラーを landuse で微調整する(安い / 粗い)** ★まずこちら
- `updateFarMesh` の頂点色計算で、`terrainColorRGB(h)` の結果に `landuseTypeAt(x,z)` 由来の色を **弱くブレンド**する(例:混合比0.35)。
  - `farmland/meadow/allotments` → 黄緑〜明るい緑
  - `orchard` → やや暗い緑
  - `forest` → 濃い緑
  - `residential` → 灰緑
  - `commercial/retail/industrial` → 灰色寄り
- 200m格子なので**境界はぼやける**が、「この一帯は田んぼ、あの一帯は住宅地」という**面の傾向は確実に伝わる**。
- コストはほぼゼロ(頂点数は 61×61 = 3721 で、更新のたびに `landuseTypeAt` を3721回呼ぶだけ。空間ハッシュがあるので十分速い)。
- **注意**:`landusePolygons` はタイル取得のたびに増減する(`part4.js:1090` で古いタイルの分を削除している)。頂点色を焼き込んだままだと**古い色が残る**。`updateFarMesh` を landuse 更新時にも再実行する必要がある。ここが唯一の実装上の罠。

**案B:landuse ポリゴンを薄い板として地面に重ねる(高い / 綺麗)**
- 水域ポリゴンと同じやり方で、landuse ポリゴンを三角形分割して地形の少し上(+0.05m程度)に敷く。
- 境界が正確に出て、田んぼが田んぼの形をする。
- ⚠ **これは水面と完全に同じ構造の問題を招く**:地形との z-fighting、地形が遅れて届いたときのズレ、ポリゴン予算切れ。あの10回の往復をもう一度やることになりかねない。
- **やるなら、水面の教訓をそのまま適用する**:①地形が未到着なら板を作らず再試行キューへ ②`polygonOffset` で深度だけ押し出す ③欠測を0に潰さない。

**推奨:案A だけをやる。** 案Bは効果と危険が釣り合わない。案Aで「情報量が増えた」実感が得られるかをまず確かめる。

## 触るファイル / 勘所

- `part5.js`(`terrainColorRGB` と `updateFarMesh` の頂点色ループ)、`part1.js`(`landuseTypeAt` はそのまま使える)。
- 勘所:**`terrainColorRGB` の時代モード分岐(space/edo/marchen)を壊さないこと**。landuse ブレンドは「現実・明治」だけに適用し、他モードは従来どおりが安全。

## 規模 / 完了判定

- 案A: **小**。判定 = 上空から見て、市街地・田畑・森が色で見分けられる。かつ FPS が変わらない。

---

# C-1. WebGLコンテキスト消失からの自動復帰

## 現状(実測)

`part9.js:439-443` に**ハンドラは既に存在するが、ログを出しているだけ**:

```js
renderer.domElement.addEventListener('webglcontextlost', (e) => {
  console.error('[GL] webglcontextlost が発生しました(GPU側で資源が失われた)', e.statusMessage || '');
  // …(復帰処理なし)
});
renderer.domElement.addEventListener('webglcontextrestored', () => console.warn('[GL] webglcontextrestored'));
```

⚠ **重要な仕様**:`webglcontextlost` は **`e.preventDefault()` を呼ばないと `webglcontextrestored` が発火しない**。現状のコードで preventDefault が呼ばれているかは要確認(上記の抜粋範囲には見当たらない)。呼んでいなければ、コンテキストは二度と戻らず画面が固まったままになる。

背景として、GPUクラッシュは「連続移動が発動条件、THREEのデータ量が原因ではない(実測 geo+tex 合計40MBなのにGPU 2GB)」までしか分かっていない未解決の懸案。

## 本当の課題

**根治は長期戦。だが「落ちても数秒で戻る」なら体感上ほぼ解決する。** この項目は根治ではなく「復帰」に全振りする。

## 段階案

**Phase 1 — 状態の常時保存(復帰の前提)**
- 毎秒〜数秒ごとに `localStorage` へ:プレイヤーの緯度経度、camYaw、時代モード、時間帯、PERFプリセット、アクティブなゲームモード(explore/geo/routesim)。
- これは C-4「前回の位置を復元して起動」とまったく同じ仕組みなので、**両方を1つの実装でまかなえる**。

**Phase 2 — 落ちたら自動でリロード**
```js
renderer.domElement.addEventListener('webglcontextlost', (e) => {
  e.preventDefault();                 // ← これが無いと restored が来ない
  saveResumeState();                  // Phase 1 の保存を即実行
  showOverlay('描画をやり直しています…'); // ユーザーに「壊れた」と思わせない
  // 復帰を数秒待ち、来なければリロード
  setTimeout(() => { if (!contextRestored) location.reload(); }, 3000);
});
```
- リロード後、`?resume=1` 相当のフラグを見て保存済み座標へジャンプする。
- **リロードループの防止が必須**:`sessionStorage` に復帰回数を持ち、3回連続で落ちたら自動リロードをやめて「PERFを一段下げますか?」のダイアログを出す。これが無いと、根本的に重い場所で無限リロードになる。

**Phase 3 — 真の contextrestored 対応(理想だが重い)**
- リロードせずに three.js のリソースを再構築する。全メッシュ・テクスチャ・ジオメトリを作り直す必要があり、**現在のコード構造(グローバル変数に散在)ではほぼ不可能**。
- **Phase 3 はやらない判断でよい**。リロード復帰で十分。

**Phase 4(副産物)— クラッシュの計測**
- 復帰時に「直前の位置・PERF・経過時間・タイル生成数」を localStorage に追記していけば、**未解決のクラッシュ原因を絞る実データが自動で溜まる**。これは根治への一番安い近道でもある。

## 触るファイル / 勘所

- `part9.js`(既存ハンドラの拡張)、`part1.js`(起動時の復帰処理)、`index.html`(オーバーレイのDOM)。
- 勘所:**`e.preventDefault()` の有無を最初に確認**。あと、`saveResumeState()` はコンテキスト消失後に three.js のオブジェクトを触ってはいけない(既に無効)。保存対象は**素の数値だけ**にすること。

## 規模 / 完了判定

- Phase1+2: **小〜中**。判定 = PC の DevTools で `WEBGL_lose_context` 拡張を使って意図的にコンテキストを失わせ、3秒以内に同じ場所へ戻ってくる。
- 補足:この検証は**PC の DevTools で完結する**。実機の再現待ちが要らない([[feedback_never_collapse_missing_data_to_zero]] の「PC DevToolsは最初から使えた」の教訓)。

---

# C-2. 生成の進捗を通常HUDに出す

## 現状(実測)

- デバッグ用のタイルオーバーレイが既にある:`#debugTileBtn`(🩺)→ `setDebugTileOverlay()`、色凡例 `#debugTileLegend`(`part7.js:1080-1090`、`part9.js:104-`)。
- タイルの状態は6段階(`part9.js:236-242`):

  | status | 意味 | 凡例色 |
  |---|---|---|
  | `unqueued` | まだキューにも入っていない | 灰 |
  | `fetching` | OSM取得中 | 赤 |
  | `gaveUp` | 取得を諦めた | 紫 |
  | `waitTerrain` | 道路は来たが地形待ち | 青 |
  | `buildingPending` | 建物クエリ待ち / メッシュ生成待ち | 橙 |
  | `done` | 完了 | 緑 |

- つまり **データは完全に揃っている**。足りないのは「常用者向けの見せ方」だけ。

## 本当の課題

初見のユーザー(と、久しぶりに開いた自分)は、**生成が遅れている状態と壊れている状態を区別できない**。特に Render のコールドスタート(30〜45秒)と重なると、「開いたけど何も出ない」で閉じられる。

## 段階案

**Phase 1 — 1行のステータス(推奨・最小)**
- 既存の `#status` 要素を使い、`done` 以外のタイルが1枚でもあるときだけ表示:
  - `🛰 周辺を生成中… 12/25` (done数 / 対象数)
  - 全部 done なら 3秒後にフェードアウトして消える
- 実装は `part9.js` の status 集計ループに `counts[status]++` を足して DOM に流すだけ。**新しい計算はほぼゼロ**。

**Phase 2 — 詰まりを区別して伝える**
状態によって文言を変える。ここが本当の価値:
  - `fetching` が多い → `🛰 地図データを取得中…`
  - `waitTerrain` が多い → `⛰ 地形を待っています…`
  - `buildingPending` が多い → `🏢 建物を配置中…`
  - `gaveUp` が1枚でもある → `⚠ 一部の地図データを取得できませんでした(自動で再試行します)` ← **ここが最重要**。過去に何度も「生成が止まった」と報告された状態が、はっきり言葉になる。
  - 起動直後で全部 `unqueued` かつ経過30秒未満 → `⏳ サーバーを起こしています…`(Renderコールドスタート対策の文言)

**Phase 3 — ミニマップに薄く重ねる**
- ミニマップ(`part7.js`)に、未完了タイルを薄い灰色の四角で重ねる。🩺 の簡易常用版。
- 「どっちの方向がまだ生成されていないか」が分かるので、**遅い方向へ歩かない**という行動を誘導できる。

## 触るファイル / 勘所

- `part9.js`(status 集計)、`part7.js`(ミニマップ)、`index.html`(`#status` のスタイル)。
- 勘所:**毎フレーム DOM を書き換えないこと**。集計は既存のオーバーレイ更新周期に相乗りし、文言が変わったときだけ `textContent` を更新する。

## 規模 / 完了判定

- Phase1+2: **小**。判定 = 起動直後とジャンプ直後に進捗が出て、完了すると消える。gaveUp が出たとき警告文が出る。

---

# D-1. モードの現在地を1箇所に集約

## 現状(実測)

`index.html` の HUD 要素を全部数えると、**モードに関わる軸が4つ並存**している:

| 軸 | UI | 状態表示 |
|---|---|---|
| ① 時代モード(見た目) | `#modeBtn`(`#modeIco` + `#modeSub`) | ボタン自身のサブラベル |
| ② 視点(`#viewBtn` / `#viewIco` / `#viewSub`)+ `#birdBtn` / `#birdDownBtn` / `#camDirBtn` / `#altKeepBtn` | 複数ボタン | バラバラ |
| ③ ゲームモード(`ModeRegistry`: `explore` / `geo` / `routesim`) | `#geoBtn` / `#routeBtn` | **`#geoFollowBadge` / `#routeSimBadge` / `#railLockBadge` の3つの独立バッジ** |
| ④ 設定(`#perfBtn` ⚙) | 1つに統合済み(過去にユーザー要望で統合した経緯あり) | `#perfSub` |

- `ModeRegistry`(`js/core/mode-registry.js`)は**設計は良いが、まだ `routesim` と `geo` の2つしか登録されていない**(`part10.js:274`、`part7.js:630`)。`explore` は「戻り先」としてだけ使われている。
- バッジが3つ別々にあるということは、**「今どの状態か」が画面の3箇所に散っている**。今後「電車でGO」やアルバムが増えると、バッジが5〜6個になる。

## 本当の課題

**「何が今アクティブか」の単一の真実の場所(single source of truth)が UI 上に無い。** ⚙ で設定を1つに統合した判断は正しかった。同じことをモードでやる番。

## 段階案

**Phase 1 — バッジを1つに統合(推奨・最小)**
- `#geoFollowBadge` / `#routeSimBadge` / `#railLockBadge` を廃止し、**画面上部固定の `#modeBadge` 1つ**に置き換える。
- 表示内容は `ModeRegistry.getActiveMode()` から生成:`🚶 探索` / `📡 GPS追従` / `🧭 経路シム` / `📡 GPS追従 · 🚃線路ロック`(サブ状態はドット区切りで連結)。
- タップすると解除(`switchMode('explore')`)。**「抜け方が分からない」を構造的に解決する**。
- ⚠ 現在バッジを直接触っているコードが `part7.js` / `part9.js` / `part10.js` に散っているはずなので、**まず `grep -n "Badge"` で全参照を洗ってから**着手。

**Phase 2 — ModeRegistry を本物の単一軸にする**
- `explore` を正式に `registerMode` する(今は暗黙の戻り先でしかない)。
- 各モードの `onEnter`/`onExit` にバッジ更新・専用UIの表示/非表示を集約する。**現在は各所で個別に DOM を触っている**はずなので、これをレジストリ側に寄せる。
- こうしておくと「電車でGO」もアルバムも、`registerMode` 1回で HUD に正しく現れる。

**Phase 3 — モード切替シート**
- `#modeBadge` をタップ → 下からシートが出て、登録済みモードの一覧から選べる。`#perfCtrl` のポップオーバー実装をそのまま流用できる。
- ここまでやると、`#geoBtn` / `#routeBtn` を hudRail から外せる = **ボタンが2つ減る**。

**Phase 4(将来設計)— 4分類**
- 前回メモの「歩く / 追う / 乗る / 見る」。今すぐ実装する必要はないが、**Phase 2 で `registerMode` に `category` フィールドを1つ足しておく**だけで、後から分類UIを被せられる。これは今やるべき安い保険。

## 触るファイル / 勘所

- `index.html`(バッジのDOM統合)、`js/core/mode-registry.js`(拡張)、`part7.js` / `part9.js` / `part10.js`(参照の付け替え)。
- 勘所:**視点(②)は別軸なので混ぜない**。「どのモードか」と「どこから見ているか」は独立。混ぜると分かりにくくなる。

## 規模 / 完了判定

- Phase1: **小**。判定 = GPS追従中も経路シム中も、画面の同じ場所に1つだけバッジが出て、タップで抜けられる。
- Phase2: **中**。判定 = 新しいモードを `registerMode` するだけで HUD に正しく出る(電車でGO実装時に効果が出る)。

---

# D-3. 設定(⚙)の項目名を「何が起きるか」で書き直す

## 現状(実測)

`index.html:478-494`:

```html
<button id="perfBtn" title="設定(描写・海面・時間帯・キャラクター・視点)">⚙ <span id="perfSub">標準</span></button>
…
<button id="perfLiteBtn"  data-preset="lite">…</button>
<button id="perfStdBtn"   data-preset="std">…</button>
<button id="perfHighBtn"  data-preset="high">🏙 高品質</button>
<div id="perfDesc">軽量: 建物1.4km/低負荷(スマホ向け)<br>標準: 建物2.2km<br>高品質: 建物4.2km/先読み拡大(高性能PC向け・メモリ大)</div>
```

- **過去にユーザー要望でアイコンを ⚙ 1つに統合した経緯がある**(`index.html:98`, `477` のコメント)。#seaCtrl / #charCtrl も中に入っている。この判断は既に良い。
- 説明文も既に「建物1.4km」等、ある程度は具体的。
- しかし ⚙ の中には **描写・海面・時間帯・キャラクター・視点回転** の5系統が入っていて、`title` 属性がそれをそのまま列挙している。

## 本当の課題

思っていたより状況は良い。残る問題は2つに絞られる:

1. **「建物2.2km」は距離であって結果ではない。** ユーザーが知りたいのは「落ちにくいか」「遠くまで見えるか」。
2. **⚙ の中身が5系統の寄せ集めで、見出しが無い。** 統合したのは正しいが、統合先の整理がされていない。

さらに実測から見つかった論点:
- 記録に「**std(bMax 12000)より high(bMax 25000)の方が見た目上安定**」というユーザー報告が残っていて、未対応のまま。**これが本当なら、プリセットの名前と説明が実態と逆**ということになる。文言を書き直す前に、ここを確かめる価値がある。

## 段階案

**Phase 1 — 文言を「結果」で書き直す(最小)**
```
🪶 軽量     遠くは見えないが、落ちにくい      (スマホ・古い端末向け)
🏘 標準     ふつう                            (多くのスマホ向け)
🏙 高品質   遠くの街まで見える。メモリを多く使う (高性能PC・新しいスマホ向け)
```
- 距離(1.4/2.2/4.2km)は補足として小さく残す。消さない — 自分でチューニングするときに要る。

**Phase 2 — ⚙ の中身に見出しを付ける**
- `描写` / `海面` / `時間帯` / `キャラクター` / `視点` の5つを、区切り線+小見出しで分ける。DOM の並べ替えだけで済む。
- ついでに **`🩺 タイル状況` は「デバッグ」見出しの下に隔離**する(常用者が誤って押す場所に置かない)。

**Phase 3 — プリセットの実態を確認して、必要なら定義し直す**
- 上記の「high の方が安定」報告を実機で検証する。もし本当なら:
  - `std` の bMax が中途半端に低くて**建物の生成/破棄が頻繁に往復している**(スラッシング)可能性がある。
  - その場合は数値の再設計が必要で、これは文言の問題ではない。
- ⚠ 数値をいじるときは [[feedback_pair_check_distance_constants]] のとおり、**全プリセットで不等式を計算して見せること**。

**Phase 4(任意)— 「おすすめ」の自動判定**
- 初回起動時に `navigator.deviceMemory` / `hardwareConcurrency` / WebGL の `MAX_TEXTURE_SIZE` あたりを見て、プリセットを自動で選び「おすすめ」バッジを付ける。ユーザーは変更できる。

## 触るファイル / 勘所

- `index.html`(DOM と文言)、`part1.js:40-170`(i18n 辞書。**日本語と英語の両方を直す必要がある**)。
- 勘所:**i18n を経由すること**。`data-i18n` / `data-i18n-html` 属性の仕組みが既にあるので、直接テキストを書き込まない(過去に audio.js で自前辞書を作って統合し直した経緯あり)。

## 規模 / 完了判定

- Phase1+2: **小**。判定 = ⚙ を開いた人が、数値を読まずにどれを選べばいいか分かる。ja/en 両方が正しく出る。
- Phase3: **中**。判定 = プリセット間の挙動が説明どおりになる。

---

# まとめ:改訂した着手順

コードを読んだ結果、前回の見立てから優先順位が変わった。

| 順 | 項目 | 規模 | 理由 |
|---|---|---|---|
| 1 | **C-1 Phase1+2**(状態保存+自動リロード復帰) | 小〜中 | 既存ハンドラの拡張だけ。PC DevToolsで検証完結。**Phase4の計測が未解決のクラッシュ原因究明にも効く**。副産物として C-4(前回位置の復元)も手に入る |
| 2 | **C-2 Phase1+2**(生成進捗の1行表示) | 小 | データは全部揃っている。見せ方を足すだけ。「壊れた」の誤解を潰す |
| 3 | **A-1 Phase1+2**(駅名+name付き建物) | 小 | **通信ゼロ**で世界に固有名詞が入る。費用対効果が最大 |
| 4 | **D-3 Phase1+2**(文言と見出し) | 小 | 1〜2回で終わる。ついでに Phase3 の「highの方が安定」問題を宿題として顕在化させる |
| 5 | **B-3 案A**(landuse を頂点カラーにブレンド) | 小 | landuse も頂点カラーの口も既にある。**案Bには手を出さない** |
| 6 | **D-1 Phase1+2**(バッジ統合+ModeRegistry整理) | 中 | **電車でGO / アルバムを作る前にやるべき**。後からだと移行コストが上がる |
| 7 | **B-2 Phase1**(山影レイヤーを別メッシュで追加) | 中 | 効果は大きいが、farMesh に触らない設計が必須。Phase2(camera.far)は別回に分ける |
| 8 | **A-8a**(アルバム本体) | 中 | 未実装。ここから始まる。GPS追従との掛け算(A-8c)まで行けば独立した価値になる |
| 9 | A-8b/c(散歩統合) | 小 | A-8a の後 |
| — | B-2 Phase2(camera.far 拡張) | 中・**リスク高** | z-fighting 再燃の危険。Phase1 の実機確認後に単独で |

## 全部に共通する進め方の提案

1. **1指示書=1 Phase**。今回の Phase 分けはそのまま「別チャットへの1回の依頼」の単位になるように切った。
2. **着手前に必ず grep**。今回、A-1 と B-1 が既に実装済みだったことが判明した。指示書の冒頭に「まず `grep -rn "<キーワード>" js/` で既存機構を確認せよ」と書くこと。
3. **farMesh / farNodeY / 水面まわりには触らない**。水面の合意事項が全部そこに集約されている。新機能は必ず「加算する新しいメッシュ/レイヤー」として作る。
4. **数を増やす機能には必ず上限を同時に設計する**(ラベル、写真、板ポリゴン)。
5. **PC の DevTools を最初から使う**。C-1 も B-2 の z-fighting も B-3 も、実機を待たずに PC で判定できる。
