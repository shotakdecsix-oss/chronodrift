# 実装指示 v2: 「紫(諦め)はほとんど無い」を踏まえた再分析

作成: 2026-08-03 / v1(`IMPL_PROMPT_20260803_ROAD_FIDELITY_ROOTCAUSE.md`)の優先順位を差し替える

> ユーザー観測: **オーバーレイの紫(gaveUp)はほとんど出ていない。むしろ FAR の段階で
> 道路・線路より先に手続き生成の建物・木ができていて、それに縛られているように見える。**

この観測は正しい。v1で挙げた giveUp 経路は「穴ではあるが主因ではない」に格下げする。
以下の3つの機構は **giveUp を一切必要とせず**、通常の探索・移動だけで症状を生む。

---

## M1【本命】way の所属タイル判定が「最初のノード」なので、タイル作り直しで長い way が永久に消える

### 事実(コードで確認済み)

**登録側** — `part8.js:1598-1607`(processTileData 末尾)

```js
seenOSMWays.add(el.id);
const g = (el.geometry && el.geometry[0]) || ...;   // ★ way の「最初のノード」
const k = osmTileKeyOfXZ(pxz.x, pxz.z);
tileWays.get(k).push(el.id);                        // ★ その1タイルにだけ帰属させる
```

**破棄側** — `part1.js:1720-1733`(reviveStaleTiles)

```js
if (!targets.has(osmTileKeyOfXZ((r.x1 + r.x2) / 2, (r.z1 + r.z2) / 2))) { ...生存... }
// ★ 道路レコードは「セグメント中点の位置」でタイルに割り当てて捨てる
```

**再取得側** — `part8.js:266`(resetTileForRefetch)

```js
const ways = tileWays.get(key);
if (ways) { for (const id of ways) seenOSMWays.delete(id); }  // ★ 帰属タイルの way しか un-see しない
```

**帰属が「最初のノード」なのに、破棄は「セグメントの位置」。この2つが食い違っている。**

### 何が起きるか

1. 密集地で `evictFarDormant` が DORMANT_MAX(60000)超過分を捨てる
   → `markTileStale(B)`(**密集地では日常的に起きる**。過去ログで「2秒あたり1〜2万件」)
2. `reviveStaleTiles` → タイルBの道路レコードを**位置で全削除** + `dropTileRemnants(B)`
3. `resetTileForRefetch(B)` は「最初のノードがBにある way」しか un-see しない
4. タイルBを再取得 → 長い線路 way W は `seenOSMWays.has(W)` で**素通り**
5. **結果: タイルBの線路は永久に消えたまま。しかもタイルBは road/building ready(緑)。gaveUp(紫)にはならない。**
6. `addRoadRecord` が呼ばれないので、**事後清掃(対策2・3)は原理的に一度も発火しない**
7. チャンクゲートは通るので、その空白に手続き生成の家・木が入る

### なぜ「線路が特に酷い」のか — これが決定的な傍証

線路は1本の way が数km〜数十km続く。道路(特に住宅街の細街路)は1本が数十〜数百m。
**way が長いほど「最初のノードが別タイルにある」確率が上がる**ので、
この帰属バグは**線路と幹線道路を狙い撃ちで壊す**。
ユーザーの「線路が途切れる」という報告の偏りと完全に一致する。

> この件は `[[project_isehara_game_tile_refetch_loop]]` に
> 「way帰属(otk化)は未着手の本命」として既に記録されている。**まだ手を付けていない。**

---

## M2 面ポリゴン由来の木には、ゲートが一切ない(FAR で先に生える)

`handleAreaFeature`(`part4.js:810-877`)は `processTileData` から**タイル到着時に即時実行**される。

```js
} else if (isForest) {
  scatterTreesIn(poly, 130, 260);   // 森ポリゴン: 最大260本を即座に散布
}
```

- **距離ゲートが無い。** 先読みは 5×5 タイル = **±3200m**。その全域で木が即座に生える。
- **readiness ゲートが無い。** `generateChunk` の `chunkTilesReady` に相当するものが無い。
- **ポリゴンはタイル外にはみ出す。** Overpass は bbox に交差する way の**全ジオメトリ**を返すので、
  森ポリゴンの bbox は平気で隣タイル・隣々タイルまで伸びる。`scatterTreesIn` はその bbox 全域に撒く。
  → **まだ1度もフェッチしていないタイルの上にも木を植えている。**
- 唯一の防御は散布した瞬間の `isOnRoad` だけ。隣タイルの線路は当然まだ知らない。
- 撒いた木はプール直行で**チャンク帰属が無く、二度と再評価されない**(`rebuildForest` の
  山の木だけは移動のたび作り直されるので自己修復するが、`addTree` 系は一発きり)。

**ユーザーの「FARで先に木ができて縛られている」は、この経路のことで間違いない。**
`generateChunk`(120m格子・厳格ゲート・約960m以内)とは完全に別世界の、ノーガード経路が並走している。

---

## M3 事後清掃キューは、原理的にスループットが2〜3桁足りない

`_doRemoveVegetationOverlappingRoad` は **1エントリ(道路セグメント1本)ごとにプール全件を線形走査**する。

| プール | 上限 |
|---|---|
| treeTrunkP | 30,000 |
| treeTopPools × 3 | 30,000 × 3 |
| forestTrunkP | 2,600 |
| forestLeafPools × 3 | 1,000 × 3 |
| scrubP | SCRUB_MAX |
| **合計** | **約125,600 インスタンス/エントリ** |

一方、投入されるエントリ数は **道路セグメント1本につき1件**。密集タイル1枚で数千本、
先読み25枚で**数万件**が一気に積まれる。

処理側は `VEG_CLEANUP_BUDGET_MS = 4`、しかも時計チェックが `(i & 3) === 0` なので
**最低4件は必ず実行**する = 実測で1フレーム約4件 ≈ **240件/秒**。

→ **滞留は分〜時間単位。歩き続ける限り永久に追いつかない。**
→ おまけに「4件まとめて実行」が 15〜25ms のフレームスパイクを作る(フリーズ対策のはずが逆効果)。

つまり **M1で道路が届かない区画は永久に、M1を免れた区画でも数分〜数十分、木は残る。**

---

## 主因を1回の観察で確定する方法

途切れている線路を見つけたら、**そこに何が建っているか**で機構が分かる。

| 見えているもの | 意味 | 機構 |
|---|---|---|
| 線路が全く描画されず、手続き生成の**家**が建っている | 道路レコードが存在しない(存在すれば清掃は同期実行で即消える) | **M1確定** |
| 線路は描画されているのに、その上に**木だけ**乗っている | レコードはある。清掃が追いついていないだけ | M3(または植生マージン) |
| タイル表示は**緑**なのに何も無い | 取得済み扱いなのにデータが無い | **M1確定** |
| 紫のまま何分も変わらない | giveUp から復帰していない | v1の修正1 |

`console` に `[staleTile] Nタイルを作り直します` が出ていれば **M1 の発火経路が実際に回っている証拠**。
これが頻繁に出ているなら、M1 が主因と断定してよい。

---

## 修正指示(v1から優先順位を差し替え)

### 修正A【最優先】way の帰属を「通過する全タイル」にする(otk化)

**最小で正しい実装:**

1. `roadRecord` に `wayId` を持たせる(`addRoad` の引数に追加 → `rec.wid = el.id`)。
   面ポリゴン・建物レコードにも同様に持たせる。
2. `tileWays` の登録を「最初のノードのタイル」から「**そのwayのジオメトリが通過する全タイル**」に変える。
   登録側だけの変更で、1つの way が複数タイルのリストに載る。

```js
// part8.js processTileData 末尾
const seenTiles = new Set();
for (const g of el.geometry) {
  const p = latLonToXZ(g.lat, g.lon);
  seenTiles.add(osmTileKeyOfXZ(p.x, p.z));
}
for (const tk of seenTiles) { (tileWays.get(tk) || tileWays.set(tk, []).get(tk)).push(el.id); }
```

3. `resetTileForRefetch(key)` で un-see する way については、
   **そのwayが生成した全セグメントを(タイル境界を無視して)必ず消す**。
   でないと再取得で二重生成になる。`rec.wid` があれば1パスで消せる。

```js
// dropTileRemnants の中で、位置ベースの削除より先に実行する
const unseen = new Set(tileWays.get(tk) || []);
// roadRecords から wid ∈ unseen のレコードを全部落とす(タイル外の分も含めて)
```

4. `evictFarRoads` の「全滅タイルのフラグ落とし」も同じ規律に揃える。

**受け入れ基準**: 密集地(渋谷・新宿等)で数分歩き回り、`[staleTile]` ログが出た後に
その区画へ戻って**線路が復活すること**。現状は復活しない。

### 修正B 面ポリゴン由来の木を、即時散布からチャンク方式へ移す

`handleAreaFeature` では**ポリゴンの登録だけ**を行い、木は撒かない。
木の散布は `generateChunk` 側(既に readiness ゲート済み・チャンク帰属あり)へ移す。

- 得られるもの: (a) ゲートが効く (b) チャンク帰属が付く (c) 再訪で二重にならない
  (d) 遠方で無駄な木を作らずプール枠を温存できる
- 移行が重い場合の暫定緩和(こちらでも症状は大きく減る):
  `scatterTreesIn` の実行条件を「**そのポリゴンの bbox を覆う全タイルが road ready**
  かつ プレイヤーから BUILDING_GEN_DIST_PROC(1000m)以内」に絞り、
  不成立なら `pendingAreaTrees` に退避して、条件成立時に撒く
  (`chunkWaitBuildings` / `tileWaitBuildings` と同じ隔離キュー方式が既にあるので流用できる)。

### 修正C 事後清掃のスループットを2桁上げる

- **1セグメント1エントリをやめる。** タイル(またはバッチ)単位で受け取ったセグメント群を
  1つのジョブにまとめ、**プールを1回走査する間に全セグメントを同時判定**する。
  現状の「N回走査」が「1回走査」になり、N=数千なら3桁の改善。
- プール側に 40m セル(`ROAD_CELL` と同じ格子)の占有インデックスを持たせれば更に落ちるが、
  スワップ詰めとの整合(`pool.resnap` と同じ規律)が必要なので第2段階でよい。
- `(i & 3) === 0` の時計チェックを**毎回**に変える(現状は budget を最大6倍オーバーランする)。

### 修正D v1の「giveUp再投入」

穴であることに変わりはないので入れるが、**優先度は最後でよい**(紫が出ていない以上、発生頻度が低い)。

---

## 大原則への追記(v1の8章に足すこと)

7. **実データの同一性は way 単位。タイルは取得の単位でしかない。**
   タイル単位で破棄・再取得する処理は、way 単位の重複排除(`seenOSMWays`)と必ず衝突する。
   **「捨てる」と「un-see する」は必ず同じ単位で、原子的に行う。**
   片方が位置ベース、片方がID帰属ベース、という今回の食い違いが典型的な失敗。
8. **ゲートを持たない生成経路を1本も残さない。**
   `generateChunk` をどれだけ厳格にしても、`handleAreaFeature` のようなノーガード経路が
   並走していれば忠実性は守れない。**手続き生成物を作る関数は全て同じゲートを通す。**
9. **事後清掃は保険であって主対策にしてはならない。**
   清掃はスループットが有限(M3)なので、発生源を止める方が常に優先。
   「後で消せばいい」という設計判断が出たら、それは発生源に手を付けられていないサイン。

---

## 作業順

| # | 内容 | 効果 | 規模 |
|---|---|---|---|
| A | way帰属の otk 化 + wid ベース削除 | 線路の恒久欠落が止まる(本命) | 中 |
| B暫定 | scatterTreesIn にゲート+隔離キュー | FARで先に木が生えるのが止まる | 小 |
| C | 清掃ジョブのバッチ化 + 時計チェック修正 | 既に生えた木が実際に消えるようになる | 小〜中 |
| B本体 | 木の散布を generateChunk へ移設 | 二重化・帰属問題も同時解決 | 中 |
| D | giveUp 再投入 | 残った穴を塞ぐ | 小 |

**A を単独で出し、`[staleTile]` ログが出た区画へ戻って線路が復活するかを確認すること。**
ここが直らない限り、清掃側(C)をいくら速くしても消すべき対象が現れないので効果が出ない。
