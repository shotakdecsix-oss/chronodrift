# 改善アイデア検討メモ(2026-08-19)

> 実装は別チャットに委譲する前提の「候補出し」。各項目に **規模感** と **効果の見立て** を付ける。
> 対象は現行の3D探索アプリ(`index.html` + `js/legacy/part1〜10.js`)。GAME_DESIGN.md / IDEA_WORLD_CANVAS.md の構想は「中長期」側に含む。

---

## 0. まず全体の所見(これが一番大きい改善かもしれない)

直近1ヶ月半のプロジェクト履歴は、ほぼ全部が**水面(川・海)の描画バグの往復**で埋まっている。修正回数は水没・浮上だけで10回以上。

- 現在は「水面ポリゴン内側の地形セルに穴を開け、足場は `surfaceY = max(地形, 水面)` で判定する」という根治設計に決着済み(2026-08-04)。海側も v16〜v19 まで進んで実機未確認のものが残っている。
- **提案:未確認分の実機検証を一気に片づけて、この領域を「完了」と宣言し、以後は多少の見た目の粗さが残っても手を入れない、と決める。**
- 理由:水面は「歩いていて気持ちいいか」への寄与が、下記A/Bの施策に比べて明らかに小さい。ここに投資し続けている限り、体験そのものは前に進まない。

もう一点。`js/legacy/part8.js` が193KB、part1が150KB。バグ調査のたびに巨大ファイルを読むコストが全部の作業に乗っている。**HANDOVER.md にある段階的な分割を「いつかやる」から「次の3ヶ月の柱の一つ」に格上げする**価値がある(→ C-中長期3)。

---

## A. 体験・遊びの厚み

### 小さく効くもの

**A-1. 現在地の「地名」を常時表示する** ★最優先候補
歩いていると HUD に現在の市区町村・町名、近くに名所があればその名前が出る。実際に足を動かして地名が切り替わる感覚は、それだけで「現実の上を歩いている」手応えになる。
- 実装:既存のOverpassクエリに `place=*` / `historic=*` / `tourism=*` のノードを追加し、最近傍を拾うだけ。新しい機構は不要。
- 規模:小。効果:大(現行アプリの「無言さ」を一番安く埋められる)。

**A-2. 発見スタンプ(ランドマーク・コレクション)**
名所・駅・神社などの半径50m以内に入ったら「発見」として localStorage に記録し、一覧画面で見返せる。
- GAME_DESIGN §14 では「保留」扱いだが、あれは文明育成シムの中での話。**現行の探索アプリ単体では、目的のない散歩に目的を与える最も安い手段**。
- 写真アルバム機能(FEATURE_PHOTO_ALBUM.md)と「自分だけの記録が貯まる」という体験の筋が完全に一致するので、UIも共有できる。
- 規模:小〜中。効果:大。

**A-3. 目的地ピン + 方向コンパス**
地名検索した場所に「飛ぶ」だけでなく「ピンを立てる」を追加。以後、HUDに方角と残距離が出続ける。
- 既存の `searchPlaceJump` とミニマップの視界コーン描画をそのまま流用できる。
- GPS追従モードで屋外を歩くときに、そのままナビとして機能する(モードC の簡易版を先取り)。
- 規模:小。効果:中〜大。

**A-4. 歩いた軌跡を残す**
セッション中の移動をミニマップに線で残し、総距離・最高標高を表示。GPS追従モードとの相性が特に良い。
- 規模:小。効果:中。

**A-5. 時代モード切替に演出をつける**
現実/明治/江戸/メルヘン/宇宙の切替が瞬間で入れ替わるなら、1秒程度のクロスフェードやホワイトアウトを挟むだけで「時間を移動した」感が出る。
- 規模:小。効果:小〜中(安いわりに印象が変わる)。

### 中長期の柱

**A-6. 「電車でGO」モードを次の柱にする** ★推奨
FEATURE_GEO_NAV_MODES.md の優先順位で3番目、まだ未着手。しかし:
- 線路データ(`railway` セグメント、`globalStationPoints`)は既に取得済みで新規通信ゼロ。
- 経路自動追従エンジンはモードB(経路シミュレーション)で構築済み、経路ソースを差し替えるだけ。
- 何より **「座って車窓を眺める」体験は、これまで積み上げた地形・建物・水面・音の投資が最も報われる遊び方**。歩行と違い、プレイヤーが操作をミスして変な場所に立ち往生することもない。
- 直近で「🚃 線路ロック」トグルも実装済みで、線路まわりの土台は揃っている。
- 規模:中。効果:大。

**A-7. 文明育成シム(GAME_DESIGN)は「垂直スライス1本」から始める**
現状の設計書は §5〜§16 まで決定事項が非常に厚く、逆に着手の壁が高くなっている。全システムを揃えるのではなく、**因果が1本だけ通る最小構成**を先に動かすことを提案:

> 大山(山)と相模川(川)の2地域だけ存在 → 大山に「食料が足りない」欠乏が表示される → プレイヤーが2点を結ぶ道を1本引く → フローが流れて繁栄が上がる → 大山の集落の建物ビジュアルが1段進化する

これだけでコアループの「観察→発見→解決→繁栄」が体験できるかどうか判定できる。面白くなければ設計を捨てる判断も早くできる。
- 規模:大。効果:未知(だからこそ小さく試す価値がある)。

**A-8. 写真アルバムと散歩の統合**
アルバム(EXIF位置マッピング)が実装されたら、「歩いていて過去の写真の撮影地点に近づいたら、その写真が空中に浮かんで見える」まで繋げる。IDEA_WORLD_CANVAS の③が単独で価値を持つ、という見立てを一番素直に形にしたもの。
- 規模:中。効果:大(他の誰も持っていない体験になる)。

---

## B. 見た目・没入感

### 小さく効くもの

**B-1. 時刻連動の太陽・空・霧** ★最優先候補
太陽の位置・光の色・空のグラデーション・fog の色を、実時刻(または現地時刻)に連動させる。朝焼け/昼/夕景/夜。
- 3D表現で最も費用対効果が高い定番。地形も建物も既にあるので、光を変えるだけで別のゲームに見える。
- 注意:**過去に「窓の明かりのランダム化」は試して削除された経緯がある**(GPUクラッシュ対策)。夜景を作るときはそれを蒸し返さない形(空と光だけ、建物側は触らない)にする。
- 規模:小〜中。効果:特大。

**B-2. 遠景の山影**
標高データは既に持っているので、描画距離の外側(2500m以遠)を粗いメッシュ1枚で描く。丹沢や富士のシルエットが出るだけで「伊勢原にいる」感が桁違いになる。
- 既存の「遠景海面プレーン」と同じ発想で、同じ場所に足せる可能性が高い。
- 規模:中。効果:大。

**B-3. 地面を landuse で塗り分ける**
今は遠くまで同じ緑になりがち。田・畑・森・市街地・グラウンドで色を変えるだけで、上から見ても歩いても情報量が増える。森・公園のポリゴンは既に取得済みなので、対象タグを増やすだけ。
- 規模:小。効果:中〜大。

**B-4. 環境音 Phase2**
Phase1(波・風・足音)は実装済み・実機未確認。次は「駅の近く/踏切/川のせせらぎ/鳥」を位置連動で。ただし **Phase1の実機確認が先**。
- 規模:小。効果:中。

### 中長期の柱

**B-5. 水面は「終わらせる」(→ §0参照)**
新しい改善ではなく、撤退判断としての提案。

**B-6. 時代モードのビジュアルを country profile の仕組みに乗せる**
国・地域別の建物スタイル機構が既にある。時代(江戸/明治)も「別のプロファイル」として同じ仕組みで扱えるなら、モードごとの個別実装が消えてメンテが軽くなる。
- 注意:過去に「装飾は detailOK() だけでなく専用の上限+高さゲートが要る」という教訓があるので、プロファイルを増やすときは必ず予算も一緒に設計する。
- 規模:中。効果:中。

**B-7. 近距離のみの影(shadow map)**
モバイルGPUの負荷とのトレードオフが大きく、GPUクラッシュの前科があるので**優先度は低め**。やるならプリセットの high 限定・オプトインで。
- 規模:中。効果:中。リスク:高。

---

## C. 安定性・パフォーマンス

### 小さく効くもの

**C-1. WebGLコンテキスト消失からの自動復帰** ★最優先候補
`webglcontextlost` を拾って、現在位置・モードを保存 → 自動リロード → 元の場所に復帰。
- クラッシュの根治は「連続移動が条件」までしか分かっておらず長期戦。だが**「落ちても3秒で戻ってくる」なら体感上はほぼ問題でなくなる**。緩和策として投資対効果が非常に高い。
- 規模:小〜中。効果:大。

**C-2. 生成の進捗を通常HUDに出す**
デバッグタイルオーバーレイは既にあるが、常用向けではない。周辺タイルの取得率を小さなバー1本で出すだけで、「壊れた」と誤解して待たずに諦める事態が減る。
- 規模:小。効果:中。

**C-3. FPSに応じた品質の自動降格**
一定FPSを下回り続けたら描画距離・建物上限を自動で1段下げる。ユーザーが⚙を開いてプリセットを選ぶ必要をなくす。
- 補足:過去に「std(bMax 12000)より high(bMax 25000)の方が見た目上安定」というユーザー報告が記録として残っていて未対応。自動降格を入れる前にこの矛盾の正体を掴んでおいたほうがいい(降格が逆効果になる可能性がある)。
- 規模:中。効果:中。

**C-4. 前回の位置・モードを復元して起動**
localStorage に最後の座標とモードを保存し、次回起動時に「前回の場所から続ける / 現在地から始める」を選ばせる。
- 規模:小。効果:中。

### 中長期の柱

**C-5. Overpass依存からの脱却(ベクトルタイル方式への転換)** ★最大の構造改善候補
これまでのトラブルの相当部分が Overpass 起因で説明できる:
- レイテンシSLOに対して道路が実測3.6〜3.9倍・建物が2倍
- overpass-api.de の不安定さ(HK/上海の生成失敗)
- Render のIPがOverpassに拒否されて生成停止
- ミラー切替の実験、並列度チューニング、キュー詰まり対策 … に費やした膨大な作業

**根本原因は「Overpass はリアルタイム配信用のAPIではない」こと**。事前に切り出した静的ベクトルタイル(PMTiles / MBTiles など、planet.osm 由来)を CDN か Render 上に置いて読む方式に変えれば、レイテンシ・失敗率・並列度の問題がまとめて消える可能性が高い。
- 代償:世界全体は容量が大きいので、まず日本 or 関東だけを切り出す段階的移行になる。データ更新も手動になる。
- **これは大工事だが、やる価値の見積もりが最も高い一件だと思う。まず「関東だけPMTilesにして道路レイヤーだけ差し替える」小実験から。**
- 規模:大。効果:特大。

**C-6. ホスティングの見直し**
Render無料プランのスピンダウン(起動30〜45秒)が起動時502の正体、と特定済み。有料プラン化 or 常時起動できる別ホスティングへの移行で、初回起動体験が丸ごと改善する。金額次第の判断。
- 規模:小(判断のみ)。効果:中〜大。

**C-7. legacy/part*.js の分割**
part8=193KB、part1=150KB、part9=97KB。この巨大さは全てのバグ調査・全ての機能追加に固定コストとして乗っている。HANDOVER.md の段階的抽出方針に沿って、まず**新しい機能だけは `js/core/` 側に置く**という運用ルールから始めるのが現実的(audio.js は既にそうなっている)。
- 規模:大(ただし段階的にできる)。効果:大(間接的)。

---

## D. UI / 操作性

### 小さく効くもの

**D-1. モードの現在地を1箇所に集約** ★最優先候補
歩行 / GPS追従 / 経路シミュレーション / (今後)電車 / 写真アルバム と増えてきて、「今どのモードにいるか」「どうやって抜けるか」が分かりにくくなりやすい。
- 画面の同じ場所に常時「現在のモード名」を出し、そこをタップすると全モードの切替シートが開く形に統一する。
- 規模:小。効果:中〜大。

**D-2. 操作系を親指の届く下端に寄せる**
スマホ縦持ち片手前提でボタン配置を見直す。タップ判定は既に touchend 起点に統一済みなので、位置の話だけ。
- 規模:小。効果:中。

**D-3. 設定(⚙)の項目名を「何が起きるか」で書き直す**
`bMax` や描画距離の数値ではなく、「遠くまで見える(重い)/ 近くだけ(軽い・落ちにくい)」のような言い方に。
- 規模:小。効果:小〜中。

**D-4. 初回の3画面チュートリアル**
「動かし方」「現在地に飛ぶ」「モード切替」の3枚だけ。初見で何もできずに閉じられるのを防ぐ。
- 規模:小。効果:中。

**D-5. ミニマップの北固定/進行方向固定トグルと縮尺変更**
- 規模:小。効果:小〜中。

### 中長期の柱

**D-6. モード体系の再設計**
機能が増えるほどモードが線形に増える今の構造は、いずれ破綻する。「歩く / 追う(GPS) / 乗る(電車・経路) / 見る(アルバム・戦略マップ)」の4分類に整理して、各分類の中で切り替える二段構成にしておくと、今後の追加(World Canvas、文明シム)が入る場所が最初から決まる。
- `js/core/mode-registry.js` が既にあるので、その延長として設計できる。
- 規模:中。効果:中(将来コストの削減)。

---

## E. 推奨する着手順(私の見立て)

| 順 | 項目 | 理由 |
|---|---|---|
| 1 | §0 水面の実機検証を終わらせて撤退宣言 | これを閉じないと他が始まらない |
| 2 | **B-1 時刻連動の空と光** | 単位工数あたりの印象変化が最大 |
| 3 | **C-1 WebGL自動復帰** | クラッシュ根治を待たずに体感を救える |
| 4 | **A-1 地名表示 + A-3 目的地ピン** | 「無言のマップ」に意味を与える最小セット |
| 5 | **D-1 モード表示の集約** | この後モードが増える前にやっておく |
| 6 | **A-6 電車でGOモード** | 既存資産が最も報われる、次の「柱」 |
| 7 | B-2 遠景の山影 / B-3 landuse塗り分け | 見た目の第二波 |
| 8 | **C-5 PMTiles小実験(関東・道路のみ)** | 効果は最大だが助走が要る。並行で調査開始 |
| 9 | A-2 発見スタンプ / A-8 アルバム統合 | 「自分の記録が貯まる」系をまとめて |
| 10 | A-7 文明シムの垂直スライス1本 | 上記が固まってから、面白さの判定として |

---

## F. 別チャットに渡すときの注意(過去の教訓より)

- **地図データの忠実性が最優先**。実データ(道路/線路/建物)は手続き生成やパフォーマンス都合より常に優先する。
- **実装前に既存の類似機構を grep する**。過去に、既にある仕組み(海面固定・スライダー等)を知らずに新規実装してしまった例がある。
- **欠測値を `|| 0` で潰さない**。橋の水没バグを6回すり抜けさせた真因。
- **装飾・オブジェクトを増やす機能には、必ず専用の個数上限を一緒に設計する**(detailOK() だけでは足りない)。
- **距離定数は単独でなく対で検算する**。全プリセットで不等式を確認して見せる。
