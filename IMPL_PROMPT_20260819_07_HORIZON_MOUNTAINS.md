# 実装指示 ⑦:遠景の山影を出す(B-2)

## 着手前に必ず

```
grep -n "FAR_SIZE\|FAR_SEGS\|FAR_STEP\|farMesh\|farGeo\|terrainMat\|farNodeY\|farSurfaceY" js/legacy/part5.js
grep -n "WIDE_HALF_LAT\|WIDE_SEGS\|WIDE_W\|WIDE_D\|wideElev\|loadWideTerrain\|seaMesh\|SEA_Y" js/legacy/part6.js
grep -n "WORLD_FOG\|BASE_FOG_DENSITY\|PerspectiveCamera\|buildSky\|MODE_CONF" js/legacy/part1.js
```

## 前提(調査済みの事実。**ここが想定と違っていた**)

| 項目 | 実測値 | 場所 |
|---|---|---|
| 地形メッシュ `farMesh` | `FAR_SIZE = 12000` → **半径6km**、`FAR_SEGS = 60` → 200m格子 | `part5.js:37-49` |
| 広域標高データ `wideElev` | `WIDE_HALF_LAT/LON = 0.13` → **±約14km**、`WIDE_SEGS = 28` → 約1km間隔 | `part6.js:30-33` |
| カメラの far | **5000**(space のみ 5800) | `part1.js:469` |
| フォグ | `FogExp2`, density = **0.00056**(明治 0.0004 / 宇宙 0.00008) | `part1.js:461` |
| 空 | `SphereGeometry(4000)`、`fog: false`、カメラ追従 | `part1.js:471-500` |

**地形データもメッシュも既に十分遠くまである。山が見えない原因はフォグ。**

FogExp2 の減衰 `exp(-(density·d)²)` を density 0.00056 で計算すると:

| 距離 | 残る割合 |
|---|---|
| 1,000 m | 73% |
| 2,000 m | 29% |
| 2,500 m | 15% |
| 3,000 m | 6% |
| 4,000 m | 0.6% |
| 5,000 m | **0.04%(実質完全に消える)** |

大山は伊勢原駅から約7〜8km。**メッシュの外(6km超)であり、仮にメッシュがあってもフォグで完全に消えている。**

そして重要な制約:**このフォグは近景のポップイン(生成の遅れ)を隠すために意図的に濃くしてある**(`part1.js:459` のコメントに明記)。**単純にフォグを薄くしてはいけない。** 建物が虚空から湧く様子が丸見えになる。

---

## Phase 1:山影レイヤーを *別メッシュ* として追加する(このタスクの本体)

### 方針

既存の `farMesh` には**一切手を入れない**。**純粋に加算する新しいメッシュを1枚足す。**

- 名前:`horizonMesh`(仮)
- 覆う範囲:**半径 6km 〜 25km のリング**(内側は `farMesh` が担当するので穴を開けるか、`farMesh` を上に描いて隠す)
- 解像度:1km格子程度。頂点数は数百〜千程度で十分(遠景なので粗くてよい)
- 高さの元データ:**`wideElev`(±14km)を流用**する
- マテリアル:
  - **`fog: false`** ← これがこのタスクの核心
  - 色は空の色に寄せた青灰色のシルエット(単色〜2色グラデ)
  - `depthWrite: false`
  - `renderOrder` は **空(-2)と地形(0)の間**
- カメラ追従(`farMesh` と同じく中心をプレイヤーにスナップ)

これで「**近景はフォグで守られたまま、地平線に山のシルエットだけが浮かぶ**」が両立する。

### データが足りない範囲について

- `wideElev` は ±14km までしかない。それより外(富士山は約60km)を出したい場合は、**さらに粗い third tier(例:±60km、5km格子)を起動時に1回だけ取得**する必要がある。
- **Phase 1 ではこれをやらない。** まず ±14km の範囲(= 大山・丹沢)でシルエットが出ることを確認する。富士山は別タスク。

### 絶対にやらないこと

- **`farMesh` / `farNodeY` / `farSurfaceY` / `getGroundY` には一切触らない。** 水面・地形・海岸線・汀線ランプの合意事項がすべて `farNodeY` に集約されている([[project_isehara_game_sea_coastline]] の v16〜v19)。ここを触ると水面問題が再燃する。
- `horizonMesh` を**足場判定に使わない**。見た目専用。プレイヤーは絶対にそこまで行かない(カメラ far の外)。
- フォグの density を変えない。

---

## Phase 2:カメラの far を伸ばす(**別回にすること**)

**⚠ Phase 1 を単独でデプロイし、実機で山影が出ることを確認してから着手すること。**

- `horizonMesh` は `camera.far`(現在 5000)の外には置けない。25km まで見せたいなら far を 30000 程度に伸ばす必要がある。
- **深度バッファの精度が犠牲になる。** near = 0.5 / far = 30000 は比 60,000:1。既に水面と地形の z-fighting に `polygonOffset` で対処している(`part5.js:25-28`)ので、**far を伸ばすと z-fighting が再発するリスクが高い**。
- 対策の候補(この順で検討):
  1. **`camera.near` を 0.5 → 1.0 に上げる。** これだけで深度精度は倍になる。一人称視点なので実害はほぼ無いはず。
  2. `logarithmicDepthBuffer: true` を検討する。ただしモバイル対応と性能への影響を必ず確認すること。
- **判定基準:山影が見える かつ 水面・橋・道路・海岸線で z-fighting(ちらつき)が発生していないこと。** 少しでもちらついたら revert して Phase 1 だけに戻す。

### Phase 2 を避ける代替案

far を伸ばさずに済ませたい場合、`horizonMesh` を**カメラの近く(例:半径 4500m)に置いて、遠くの山を投影した形に縮小して描く**(スカイボックスに山を焼き込むのに近い発想)。歩いても視差が出ないが、遠景なので実用上ほとんど気づかれない。**Phase 2 のリスクを取りたくない場合はこちらを検討する価値がある。**

---

## Phase 3(任意):大気遠近

- 距離に応じてシルエットの色を空の色へブレンドすると一気に「らしく」なる。
- **頂点カラーで距離を焼き込むだけでよい。シェーダ改造は不要。**
- 時間帯(朝焼け・夕景)に応じて色を変えると効果が大きい。`applyTimeOfDay` に相乗りする。

---

## 完了判定

- Phase 1:
  1. 伊勢原の平地から北西を向くと、**大山〜丹沢のシルエットが見える**。
  2. 近景のポップイン(建物が湧いて出る様子)が Phase 1 前と変わっていない = フォグが効いたまま。
  3. FPS が落ちていない。
  4. **水面・橋・海岸線の見た目が一切変わっていない。**
  5. 遠方(NY・香港など)へジャンプしても破綻しない(データが無ければシルエットが平坦になるだけで、エラーにならないこと)。
  6. 時代モードを切り替えても破綻しない(宇宙モードは別扱いにしてよい)。
- Phase 2:
  7. 上記すべてに加えて、**z-fighting が一切発生していない**(水面・橋・道路を実機で重点確認)。
